// Package wire exposes the dedicated recording-session constructor alongside
// the existing recording service injector. The implementation is kept in its
// own generated source so the generic providers.go/wire_gen.go graph remains
// byte-for-byte stable for existing hosts.
package wire
