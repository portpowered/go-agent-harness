package agentruntime

// Deprecated: filesystem safety, bounded reads, digest validation, capture
// admission, and timeline validation are implemented by the private
// roomreplaybundle runtime service. This package marker preserves the CLI
// source seam while keeping no second integrity implementation in agent-cli.
