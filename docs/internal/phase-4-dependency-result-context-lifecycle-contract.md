# Phase 4 Dependency Result Context Lifecycle Contract Map

This is the reviewer-facing convergence map for
`phase-4-dependency-result-context-lifecycle-contract`. It extends the earlier
`phase-4-dependency-result-contract-repair` evidence with the remaining
dependency, result, context, replay, prompt-resolution, and session-lifetime
surfaces that must be tracked before `P4-GATE-01` can close.

This map depends on `phase-4-audit-validator-015-reconciliation`. That
reconciled row artifact is not present in this checkout, so this document does
not mark `P4-GATE-01` as pass. Rows below use current declarations, docs, and
credential-free tests as evidence, and keep unresolved or compatibility-staged
work explicit.

## Surface Convergence

| Surface | Status | Public behavior and evidence | Checklist rows | Remaining work |
| --- | --- | --- | --- | --- |
| `agentloop.ExecuteResult.FinalText()` | Repaired | `go-agent-loop/pkg/agentloop/execute_result.go` exposes `FinalTextResult` with `Status`, `Text`, `Err`, `Partial`, and `TerminalSource`. It distinguishes non-empty success, explicit empty success, missing final assistant text, caller cancellation/deadline, terminal failure, partial output, and provider-authored versus loop-synthesized terminal boundaries when `MESSAGE.END` was observed. `ExecuteResult.Text()` remains the compatible string-only helper. Tests in `go-agent-loop/pkg/agentloop/execute_result_test.go` and `agent_loop_test.go` cover these outcomes without provider credentials. | `P4-API-01`, `P4-API-03`, `P4-GATE-01` | None for the mapped final-text contract. Keep `Text()` compatibility until a future breaking release. |
| `agentloop.Stream.Outcome()` and `messages.MessageEndValue.TerminalSource` | Repaired | `go-agent-loop/pkg/agentloop/execute_result.go` exposes `StreamOutcome` with `Status`, `Err`, `Partial`, and `TerminalSource`. It distinguishes open, drained, caller-closed, cancelled/deadline, failed streams, partial output, and provider-authored versus loop-synthesized `MESSAGE.END` boundaries after iteration. `messages.MessageEndTerminalSource(...)` normalizes legacy empty-source events as provider-authored, while `messages.NewSynthesizedMessageEndValue(...)` marks loop-authored boundaries. Existing `HasNext()`/`Response()` callers continue to compile. Tests in `go-agent-loop/pkg/agentloop/execute_result_test.go`, `participants/model_runner_test.go`, and `agent_loop_test.go` prove drained, close, cancellation, failure, partial-output, provider-authored, and synthesized outcomes. | `P4-API-01`, `P4-API-03`, `P4-GATE-01` | None for story 003. |
| `messages.TypedBuffer.ReadContext(ctx)` and `messages.TypedBuffer.WriteContext(ctx, value)` | Repaired | `go-agent-loop/pkg/messages/buffers.go` exposes explicit read and write contracts. `ReadContext` reports caller cancellation and deadlines with `ctx.Err()`. `WriteContext` returns `BufferWriteOutcome` with `succeeded`, `cancelled`, `timed_out`, and `buffer_full` statuses. Legacy `ReadBlockingContext(ctx) (T, bool)` and `Write(ctx, value) bool` remain compatible wrappers. Tests in `go-agent-loop/pkg/messages/buffers_test.go` distinguish successful reads/writes from cancellation, timeout, and buffer-full outcomes. | `P4-API-01`, `P4-API-03`, `P4-GATE-01` | None for the mapped buffer read/write contract; keep bool helpers compatibility-only. |
| `messages.SendSessionWithOutcome(ctx, session, msg)` and provider/replay typed send methods | Repaired | `go-agent-loop/pkg/messages/session.go` adds `SessionSendOutcome`, `SessionSendStatus`, optional `SessionSendOutcomeSender`, and the compatibility adapter `SendSessionWithOutcome`. Existing `Session.Send(ctx, msg) bool` callers continue to compile. `go-llm-gateway/pkg/testing.SessionReplayer.SendWithOutcome`, `providers/openai.realtimeSession.SendWithOutcome`, and `providers/grok.grokSession.SendWithOutcome` report success, caller cancellation, timeout, closed session, full outbound buffer, and terminal failure where observable. `SessionReplayer` attaches replay mismatch errors to terminal-failure outcomes. Tests in `go-agent-loop/pkg/messages/buffers_test.go` and `go-llm-gateway/pkg/testing/session_replay_test.go` prove cancellation is distinct from buffer-full, closed-session, and terminal-failure outcomes through public APIs. | `P4-API-01`, `P4-API-03`, `P4-GATE-01` | None for repaired send outcome shape; downstream bool-only session implementations can opt into `SessionSendOutcomeSender` when they can observe richer states. |
| `go-llm-gateway/pkg/testing.SessionReplayer` replay divergence and incomplete replay | Repaired | `SessionReplayer.Outcome()` returns `SessionReplayOutcome` with `Status`, `Err`, `Expected`, and `Actual`, distinguishing open replay, successful completion, divergent outbound replay, incomplete replay, and caller-owned replay context cancellation. `SessionReplayer.Send` still terminates divergent replay and `SessionReplayer.Err()` remains compatible for error-only callers. Divergent and incomplete outcomes preserve errors wrapping `providers.ErrReplayMismatch` and `gateway.NewReplayMismatchError(...)`; expected/actual details are inspectable without log parsing. Tests in `go-llm-gateway/pkg/testing/session_replay_test.go` cover divergence, omitted expected outbound events, flushed capture replay completion, and successful completion outcomes. | `P4-API-02`, `P4-API-03`, `P4-API-05`, `P4-GATE-01` | None for story 004. |
| `agent-cli/internal/agent.Executor.LoadSystemPromptWithDetails(...)` | Compatibility-staged at CLI boundary with repaired observability | The public CLI-facing migration guide in `docs/architecture/dependency-result-contracts.md` names prompt resolution as CLI-owned composition rather than a pure library-grade prompt assembler in this repair batch. `LoadSystemPromptWithDetails(...)` reports literal prompts, prompt file reads, default `AGENTS.md` create/read behavior, config loading, runtime system-info collection, skill metadata reads, and loop suffix appends. Existing `LoadSystemPrompt(...)` remains compatible. Tests in `agent-cli/internal/agent/executor_test.go` prove the observable sources and side effects through local fixtures only. | `P4-API-07`, `P4-GATE-01` | Future pure prompt assembly should be a separate additive API with injected filesystem, config, system-information, skills, and clock dependencies. |
| Filesystem, environment, process, transport, network, and time dependency ownership | Repaired or compatibility-staged by seam | The public guide classifies filesystem prompt/config/skills/session/capture access as CLI-owned, environment lookup as CLI-owned config loading, process runtime and current time as CLI-owned system-information injection, HTTP transport/live/record/replay selection as application composition, provider network protocol behavior as provider-owned, and caller deadlines/cancellation as `context.Context` ownership. Provider packages receive resolved config and injected runtime clients rather than consulting CLI environment or transport policy directly. | `P4-API-01`, `P4-API-07`, `P4-GATE-01` | Keep open only for new surfaces that introduce hidden side effects outside these documented seams. |
| `go-llm-gateway/pkg/models.SessionConfig` and `gateway.DefaultSessionGateway.ConnectSession(ctx, config)` | Already correct at gateway/provider boundary | `models.SessionConfig` is the persistent session shape passed into `gateway.DefaultSessionGateway.ConnectSession(ctx, config)` and concrete provider `ConnectSession(ctx, config)` methods. The operation context is separate from the config value at this boundary. Tests in `go-llm-gateway/pkg/inference/session_inferencer_test.go`, `providers/openai/session_test.go`, and `providers/grok/provider_test.go` cover gateway/provider session establishment through local fakes and replay fixtures. | `P4-API-01`, `P4-API-07`, `P4-GATE-01` | None at the gateway/provider boundary. |
| `go-llm-gateway/pkg/inference.SessionGatewayInferencer` and `inference.SessionRequest` | Repaired | `SessionRequest` is the loop-facing persistent session shape for `SessionGatewayInferencer`; `ConnectSession(ctx)` keeps caller-owned cancellation and timeout scoped to one connection attempt. `WithSessionRequest(...)` accepts the full `models.SessionConfig` including modalities, audio formats, tools, turn detection, and provider-specific config. Legacy `WithSessionModel`, `WithSessionVoice`, and `WithSessionInstructions` remain compatible wrappers. `SessionGatewayInferencer` defensively copies the request on input, on `Request()`, and per `ConnectSession(ctx)`. Tests in `go-llm-gateway/pkg/inference/session_inferencer_test.go` prove full config propagation, defensive copies, and that a cancelled connect does not mutate the persistent request for a later operation. | `P4-API-01`, `P4-API-07`, `P4-GATE-01` | None for story 005. |
| Provider-authored end vs loop-synthesized end | Repaired | `messages.MessageEndValue.TerminalSource` is the public event-level metadata. `agentloop.Stream.Outcome().TerminalSource` and `agentloop.ExecuteResult.FinalText().TerminalSource` carry that value to the loop result surfaces. Provider-authored `MESSAGE.END` events remain compatible through empty-source normalization, while `participants.ModelRunner` emits `TerminalSourceLoopSynthesized` for non-streaming fallback deltas and provider stream channels that close without `MESSAGE.END`. Tests in `go-agent-loop/pkg/participants/model_runner_test.go` and `go-agent-loop/pkg/agentloop/execute_result_test.go` prove provider-authored and loop-synthesized ends are distinguishable. | `P4-API-03`, `P4-GATE-01` | None for story 003. |

## Checklist Decisions

| Row | Decision | Evidence | Follow-up before closure |
| --- | --- | --- | --- |
| `P4-API-01` | `uncertain` | Major blocking surfaces already accept caller-owned `context.Context`: loop execution/streaming, gateway inference/session connection, provider session connection, typed session send helpers, `TypedBuffer.ReadContext`, and `TypedBuffer.WriteContext`. `FinalText()`, `Stream.Outcome()`, `BufferWriteOutcome`, `SessionSendOutcome`, and `SessionRequest` expose cancellation/deadline separately from persistent session configuration. Story 006 documents hidden side-effect ownership for prompt, config, system-information, transport, network, and time seams. | Final CLI/session evidence story must finish before row closure. |
| `P4-API-03` | `uncertain` | `FinalText()` and `Stream.Outcome()` repair final text and stream terminal ambiguity, including provider-authored versus loop-synthesized `MESSAGE.END` provenance. `SessionSendOutcome` repairs send lifecycle ambiguity for implementations that can observe closed, full, cancellation, timeout, and terminal-failure states. `SessionReplayOutcome` distinguishes replay completion, divergence, incomplete replay, and cancellation while preserving typed replay mismatch errors. | Remaining stories must finish session config and prompt/dependency ownership surfaces before row closure. |
| `P4-API-07` | `uncertain` | Provider HTTP runtime and prompt resolution ownership are documented in `docs/architecture/dependency-result-contracts.md`; `models.SessionConfig` is explicit at the gateway/provider boundary; `inference.SessionRequest` makes the loop-facing persistent session shape explicit; prompt details expose CLI IO side effects. Story 006 explicitly keeps prompt composition compatibility-staged at the CLI boundary rather than introducing a pure assembler in this batch. | Final evidence story must reconcile these decisions against the row artifact before closure. |
| `P4-GATE-01` | `uncertain` | This map, the public dependency/result guide, and existing credential-free tests give reviewer-visible evidence for repaired and staged contracts. | Do not close until `phase-4-audit-validator-015-reconciliation` rows are available and stories 002 through 007 either repair or explicitly compatibility-stage their remaining surfaces with validation evidence. |

## Reviewer Commands

Run these credential-free checks from the repository root:

```sh
make typecheck
go test ./go-agent-loop/pkg/agentloop
go test ./go-agent-loop/pkg/messages
go test ./go-llm-gateway/pkg/testing
go test ./go-llm-gateway/pkg/providers/openai ./go-llm-gateway/pkg/providers/grok
go test ./go-llm-gateway/pkg/inference
go test ./agent-cli/internal/agent
```
