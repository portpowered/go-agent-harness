# C24 CI rejection reconciliation

## Canonical inbox

The full canonical query was run at admission and again after rejection:

`rtk proxy you --server "$FACTORY_SERVER_URL" --json work list --session "~default" --max-results 500 --all`

The saved `canonical-board-rejection.json` contains the exact task and concluded
review rows. The task row `work-task-69` returned:

> CI checks failed for PR #416 at head 6775c0f31f8252893b412891053cdb43096b4878: required checks failed on current head 6775c0f31f8252893b412891053cdb43096b4878: CI (coverage)=FAILURE; checks: CI (coverage)=FAILURE (https://github.com/portpowered/go-agent-harness/actions/runs/34399798166/job/102628567810). Return this task to the executor with the listed failures.

The concluded review row `work-review-72` retained the earlier actionable
sanitization finding:

> Actionable sanitization defect in factory/scripts/ci-gate.py:62-71. _safe_text preserves ANSI/control bytes and leaks whitespace-separated bearer secrets (e.g. Authorization: Bearer TOPSECRET -> Authorization=[redacted] TOPSECRET). Adversarial review reproduced both leaks. Unit suites, graph tests, and public controls otherwise passed. Strip control/C1 characters and robustly redact authorization/token values; add regression controls, then rerun the focused suites and public matrix.

That review finding was causally repaired by `f32de8176cf54375d9fb958c8f32bf7a67232956`, with its regression evidence preserved in `sanitization-repair.md`.

## Exact CI failure

The complete failed job log for run `34399798166`, coverage job
`102628567810`, was inspected. The only failing test was:

`go-agent-loop/pkg/agentloop/TestRunCancellationReleasesBlockedDeltaForwarder`

At `run_delta_barrier_test.go:149`, the observed error was `context canceled`
while the existing assertion required a terminal `*engine.StreamDeltaError`.
The job then exited through `make coverage: Error 1`. Static, unit,
integration, race, hermetic, WebMCP Chrome, macOS-audio, and Windows-audio
checks all completed successfully in the same run.

This is not a C24-caused defect: `git diff origin/main...HEAD --
go-agent-loop` is empty and the exact test passes 20/20 in this candidate
worktree. The runtime test remains outside the admitted C24 paths, so no
runtime or assertion change is authorized. The failure is retained as exact
CI evidence for the next script-owned current-head run; it is not claimed
green or waived.

## Candidate controls after rejection

On source `6775c0f31f8252893b412891053cdb43096b4878`:

- `rtk proxy go test ./go-agent-loop/pkg/agentloop -run '^TestRunCancellationReleasesBlockedDeltaForwarder$' -count=20 -timeout=120s` — passed.
- `rtk proxy python3 -m unittest discover -s factory/scripts/tests -p test_ci_wait.py` — 23/23 passed.
- `rtk proxy python3 -m unittest discover -s factory/scripts/tests -p test_ci_gate.py` — 16/16 passed.
- `rtk proxy python3 -m unittest discover -s factory/scripts/tests -p test_factory_graph.py` — 2/2 passed.
- `rtk proxy python3 docs/temp/projects/audio-runtime/audio-runtime-c24-ci-conflict-return/public-controls.py --gate factory/scripts/ci-gate.py --cases conflict,unknown,stale-head,green --child-timeout-seconds 30` — passed with native `REJECTED`, `FAILED`, `REJECTED`, `ACCEPTED`; conflict and stale paths requested zero sleeps, UNKNOWN used bounded logical polling, and the mutation control failed closed.
- `rtk proxy python3 -m py_compile factory/scripts/ci-gate.py factory/scripts/ci-wait.py factory/scripts/tests/test_ci_gate.py factory/scripts/tests/test_ci_wait.py` — passed.
- `rtk proxy git diff --check` — passed.

The next action is to checkpoint this evidence, push the same admitted branch,
update PR #416, and return `ACCEPTED` to the script-owned CI gate without
polling its replacement run.
