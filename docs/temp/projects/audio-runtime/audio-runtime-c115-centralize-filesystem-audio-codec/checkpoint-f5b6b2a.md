# C115 checkpoint f5b6b2a

- Admission remains `audio-runtime/audio-runtime-v1`; `project-control.py verify-work --type task --name audio-runtime-c115-centralize-filesystem-audio-codec` returned `status: admitted`.
- The isolated worktree is on `codex/audio-runtime-c115-centralize-filesystem-audio-codec`, matching `prd.json.branchName`. Required accepted-main ancestry remains preserved through the pushed candidate; no host checkout reset or merge was used.
- The private filesystem adapter no longer imports the peer `audiocodec/wire`. `read_file` receives only the public `audiocodec.Service`; registry snapshots preserve that dependency, and the application composition root constructs the codec and passes it through the tools Wire injector.
- Focused evidence on this checkpoint: audio codec/tools normal and race tests passed (379 tests across 20 packages); affected CLI composition tests passed (1,869 tests across 4 packages); injected service-to-read-file regression passed; standalone `GOWORK=off` consumer test and execution passed; `make fmt`, `make vet`, pinned Staticcheck 2026.1, pinned golangci-lint 2.9.0, coverage registration (179 packages), and `make test-regressions` passed.
- `make architecture-size-check` now reports exactly one known finding: generated-file registration for `services/audiocodec/wire/wire_gen.go`, owned by C79. The former filesystem forbidden-import, peer-private-import, Wire-import, and composition-file baseline findings are resolved. The C79 shared Wire registry and architecture baseline remain untouched.
- C79 still has no reviewed guarded merge or lease release. Next action is to retain this same task and, only after C79 releases the shared files, fetch current `origin/main`, apply the demonstrated codec Wire registration/downward baseline edits, rerun the final scoped gates, and submit this same PR head to script CI without polling.
