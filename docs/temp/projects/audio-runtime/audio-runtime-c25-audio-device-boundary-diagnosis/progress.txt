C25 progress checkpoint

Admission and preservation
- Read factory/docs/operating-policy.md and factory/docs/implementation-handoff.md before action.
- Verified task admission with project-control.py: admitted to project audio-runtime.
- Confirmed prd.branchName matches codex/audio-runtime-c25-audio-device-boundary-diagnosis and the isolated worktree.
- Fetched origin/main at 5d5afcb14d7b269378020809f5a2418c499ac94d.
- Preserved baseline 3194edd97aed588f7cdf2f8c58a69ac21da4c9ad and integration 8bdafc7f947a3a2c9856220abdc539437035bd21 ancestry.
- Initial source worktree was clean; no predecessor or unrelated changes were touched.

Board/review context
- Canonical task row: work-task-71; plan work-plan-70 complete; no C25 review row; the task row later returned the PR417 CI rejection recorded below.
- Read accepted C15/C16/C17 evidence and current C18/C20/C21/C23 findings. Their owned paths and actionable review repairs remain outside this packet.

Diagnosis
- The public yui room run route uses runtimeRooms.Service from go-agent-runtime/services/rooms.
- The compiled legacy agentruntime package still contains RunRoom/RunRoomWithResult and its own internal package tests exercise them; servicetest/runtime.go imports the package for other session seams but exports no RunRoom API.
- Legacy agent-cli/internal/room/mixer.go owns time.Ticker, PCM16 codec operations, and local bounded cadence.
- Legacy session_room_orchestration.go opens gateway source/sink handles and constructs the legacy mixer.
- Legacy session_room_run.go resamples/decodes output and calls sink.WriteFrame directly.
- This is a concrete source/ownership migration hazard, not runtime/acoustic proof.

Verifier checkpoint
- verify.py --mode all at pushed candidate 0eb46135bdeb3f2976e4658a02a6f39cf1d47459: ACCEPTED in 10.325s; nine focused children, package probes, positive/negative dependency controls, stale/false-success/truncated-output controls, and intentional child-hang cleanup all passed; no child survived and shutdown was clean.
- Source gap: SOURCE_GAP_CONFIRMED.
- Positive canonical graph: ACCEPTED.
- Negative bypass graph: REJECTED_AS_FORBIDDEN.
- go list probes and all selected canonical/legacy focused checks returned 0; native return codes and harness verdicts are captured in diagnosis.json.
- Pinned source 5d5afcb14d7b269378020809f5a2418c499ac94d is an ancestor and all audited production paths are unchanged; aggregate 4.008s; child cap 55s; aggregate cap 600s; clean shutdown.

Current-head CI rejection characterization
- PR417 head 1040f92478bd1da6450f0f9a1fc1f4532ff34937, run34397773527/job102621627290, failed TestAgentBinaryToolContinuationPreservesRemoteDeviceAudio/test46/provider_burst while waitForRemoteToolAudio requested the full remote /v1/audio-device/control/snapshot after provider completion and callback-clock shutdown; the existing 30s scenario context expired at 30.04s.
- The production-binary audio-device replay and committed regression package in the same job passed. C25 read-only source tracing cannot distinguish server-lock starvation from snapshot serialization/client scheduling; no production cause is claimed.
- Local exact single-subtest passed in 12.38s and the full 12-subtest matrix passed in 29.362s. C20 owns the production/fixture repair; C25 owns only evidence and verifier paths.

Next action
- Commit and push the verifier/evidence repair and update PR417 with the exact rejection and ownership handoff. Do not poll CI. Retain C25 ownership until C20's reviewed repair (if any) is integrated, then refresh exact-source evidence and submit the changed same task through script CI.

Exact current-head refresh
- At candidate source checkpoint 3c2aea55f8c86ab293e074b50bc14edbb4837901, reran `verify.py --mode all`: ACCEPTED in 9.774s with 19 commands, no unexpected failure, source ancestry/path checks clean, and no surviving child process. The prior 0eb46135 evidence remains preserved.
- Updated the owned README, evidence ledger, extraction plan metadata, ownership checkpoint, and progress with the refreshed candidate identity. No production, shared module, fixture, baseline, device, provider, or realtime path changed.
- The hosted test46/provider_burst snapshot timeout remains the exact C20-owned failure from run34397773527/job102621627290. No C25 repair or CI-green claim is made; after this evidence checkpoint is pushed, the next action is C20 reviewed repair/main integration, then same-task C25 refresh and script-gate submission.

Current-head verifier refresh
- At candidate source checkpoint `dab5c3731b8aa23b3e71bf0e067dab4c3d4d9961`, reran `verify.py --mode all`: ACCEPTED in `13.442s` with `19` bounded commands, no unexpected failure, source ancestry/path checks clean, and no surviving child process. The 3c2aea55 and 0eb46135 evidence checkpoints remain preserved.
- This refresh changes only owned evidence metadata and generated verifier outputs. The source-only legacy-room gap, positive/negative dependency controls, focused causal regressions, and C20 ownership of the hosted snapshot timeout are unchanged. Next action remains C20 reviewed repair/main integration, then a same-task C25 refresh and script-gate submission; no CI-green claim is made.

Exact-head provenance refresh
- After the evidence checkpoint commit `ff00c0375a25a98c5d9398c8ffb5e26ccd70151b`, reran `verify.py --mode all`: ACCEPTED in `10.059s` with `19` bounded commands, clean source/path checks, and no surviving child process. Generated diagnosis/provenance now identify `ff00c037`; the prior dab5c373 evidence remains preserved.
- This is an exact-head evidence successor only; no production or shared path changed, and the hosted C20-owned snapshot timeout remains unresolved and unclaimed as CI green.

Final exact-head verifier refresh
- At candidate source checkpoint `209afd4ceca417fdca7964e3c0301ad4d3d1c250`, reran `verify.py --mode all`: ACCEPTED in `10.263s` with `19` bounded commands, clean source/path checks, and no surviving child process. Generated diagnosis/provenance now identify `209afd4c`.
- The final evidence commit will be a documentation-only successor to this verified checkpoint; the audited production paths remain unchanged. Update PR417 with the successor head and submit the same task to script CI without polling; C20's hosted snapshot timeout remains the only unclaimed red check.

Review-78 repair checkpoint
- Repaired `verify.py` to fail closed on failed/empty dependency controls and zero-test discovery, capture the actual lifecycle mixer field, verify a reproducible pinned `git archive`, verify the committed fixture manifest and exact fixture digests, and enforce a complete source-to-candidate plus working-tree changed-path allowlist.
- Corrected the service-test statement: `agent-cli/internal/services/servicetest/runtime.go:109-112` imports the legacy implementation for session helpers but exports no `RunRoom`; the legacy entrypoint is compiled and exercised by its own internal package tests.
- At current source candidate `8e96dc077744d204994274b22356af8e856a8aa7`, `verify.py --mode all` returned `ACCEPTED` in `16.275s` across `23` bounded commands. `SOURCE_GAP_CONFIRMED`, source ancestry/path equality, archive digest, fixture manifest, allowlist, dependency controls, package probes, focused causal/regression tests, race, vet, zero-test control, timeout cleanup, and clean shutdown all passed.
- Source archive rebuild and materialized copy both matched SHA-256 `e77933bdc0f47a2f885581eb9b51a297b763f7a7b54bffe3b07254fa2126f11e`; fixture manifest SHA-256 was `e640b7052eb93966be24d3016fe60995156e45cf98e2d9144a3f60c1521da95f`.
- The exact C20-owned hosted `test46/provider_burst` snapshot timeout remains preserved and unwaived. No C20 production/fixture repair was imported; no production/shared/baseline/existing-fixture path was changed. Next action is commit/push this repair, refresh evidence at the commit head, update PR417, and submit the same task to script CI without polling.

Committed repair-head refresh
- Committed verifier/control repair head: `9e61354c27ca381ed204ab05dec2aa7fd816c1d9`.
- Reran `verify.py --mode all` at that exact committed source head: `ACCEPTED` in `10.503s` across `23` bounded commands; 14 committed changed paths were inside the owned prefix, the working tree was clean, and all source, dependency, runner, package, focused causal/regression, race, vet, and cleanup evidence remained accepted.
- This evidence-only successor retains `9e61354c27ca381ed204ab05dec2aa7fd816c1d9` as the measured source candidate. After this checkpoint, push the same PR, update PR417, and submit the task to the script CI gate without polling.

Current-main refresh after C24 merge
- Fetched `origin/main` at `98ce636dd67349ba64f22cd7916dd370cf4ba484` and merged it into the isolated C25 branch at candidate `88f996c5384222d97b92578a6ba7f1ea83adbf2e`; the merge touched only C24 factory gate/docs paths and preserved all C25 evidence.
- Updated the owned verifier to pin that exact main and archive SHA-256 `e77933bdc0f47a2f885581eb9b51a297b763f7a7b54bffe3b07254fa2126f11e`; baseline `3194edd97aed588f7cdf2f8c58a69ac21da4c9ad` and integration `8bdafc7f947a3a2c9856220abdc539437035bd21` ancestry remain intact.
- `verify.py --mode all` returned `ACCEPTED` in `9.475s` with 19 bounded commands: `SOURCE_GAP_CONFIRMED`, source ancestry/path equality and production-path status accepted, all focused list/test/race/vet and dependency controls accepted, and the intentional 1-second child hang terminated with native `-15` and no surviving child.
- No C20 reviewed production/fixture repair is present on refreshed main; the historical PR417 `test46/provider_burst` snapshot timeout remains C20-owned. C25 made no production/shared/fixture/baseline/device/provider/realtime change and makes no CI-green claim.
- Next action: commit/push this exact-head evidence refresh, update PR417 with the refreshed source/candidate hashes and C20 ownership note, and submit the same changed task to the script CI gate without polling; retain C25 ownership for any exact same-head rejection.

Final pushed-head evidence stamp
- At pushed HEAD `672659326172756e26a228009560062d412c6650`, the final `verify.py --mode all` rerun returned `ACCEPTED` in `9.680s` with 19 commands, `SOURCE_GAP_CONFIRMED`, accepted source ancestry/path equality and focused controls, and no unexpected failure. The intentional child-hang control remained bounded and left no child.
- Updated `diagnosis.json`, `diagnosis.md`, `provenance.json`, `extraction-plan.json`, and `ownership.json` to identify this exact candidate. No production/shared/fixture/baseline/device/provider/realtime path changed.
- Next action: commit this exact evidence stamp, push the same PR, update PR417, and submit to the script CI gate without polling. No CI-green, merge, or project-acceptance claim is made.

Review-86 causal repair checkpoint
- The canonical review rejection for PR417 at `ecd7efc` was repaired in the
  owned verifier at commit `6151f4b7eede2a8b5a5fd9867c856a8743ddc412`.
- `dependency_controls` now invokes a Go AST import/selector oracle. The
  accepted canonical graph, rejected forbidden graph, rejected allowed-graph
  forbidden-edge mutation, and accepted comment-only mutation are all recorded
  as separate controls; malformed or empty oracle output fails closed. The
  public-route legacy-entrypoint flags are derived from inspected route source,
  and the zero-test child allowance is the bounded `55s` verifier cap rather
  than `10s`.
- A fresh `verify.py --mode all` run completed in `14.734s` with
  `SOURCE_GAP_CONFIRMED`, decision `ACCEPTED`, clean source ancestry/path and
  allowlist evidence, nine focused normal/race/vet regressions accepted, zero
  test discovery accepted, expected child-hang termination at `-15`, and no
  surviving child. No production/shared/fixture/baseline path changed.
- The next bounded action is to refresh the human-readable evidence metadata,
  commit/push the same task and PR417, verify script-gate submission without
  polling CI, and return `ACCEPTED`; retain ownership for any exact rejection.

Current-head coverage rejection and ownership handoff
- The board's current rejection for `work-task-71` is PR417 at candidate
  `b061bf852076d4b36526600377ad916751e32b4b`: `CI (coverage)=FAILURE`, run
  `34413969081`, job `102674534021`.
- The complete job log was inspected. `make coverage` failed only at
  `agent-cli/internal/wire/composition_test.go:417`: the composed session
  returned `<nil>` where the test expected the RTC media-capability error
  after preflight. The package exited 1 and coverage exited 2. Raw metadata
  and the full-log byte count/hash are retained in `ci-diagnosis.json` and
  the owned raw JSON checkpoint.
- The focused local reproduction with the same test and coverage
  instrumentation passed in `0.563s` with `4.7%` package coverage. The C25
  verifier/source evidence remains causal and accepted; C25 owns no
  production or `agent-cli/internal/wire/composition_test.go` repair.
- The exact owner is the existing C20 task/lease; C21's remote
  fixture/gateway/duplex path is not implicated. This is not an unchanged
  resubmission case. Retain C25 ownership while waiting for the reviewed C20
  repair to reach `origin/main`; then refresh the exact-head verifier and
  submit the changed same task through the script CI gate without polling.

Exact-head verifier refresh after coverage rejection
- At local checkpoint `a52291203d244d9d4f4d3a06511b9519ac65acc9`, the bounded
  `verify.py --mode all` run returned `ACCEPTED` in `14.0s` across `25`
  commands. Source/archive identity, AST dependency controls, fixture digests,
  complete owned-path allowlist, focused normal/race/vet checks, zero-test and
  child-hang cleanup controls all passed with no surviving child.
- This is an owned evidence-only refresh. The hosted composition failure remains
  at rejected CI head `b061bf852076d4b36526600377ad916751e32b4b` in C20-owned
  `agent-cli/internal/wire/composition_test.go:417`; C25 has no authorized repair
  path and will not submit unchanged implementation. Next action is a reviewed
  C20 repair integrated into `origin/main`, then a fresh C25 refresh and same-task
  script-CI handoff.

Current-main ancestry and exact-head refresh
- Fetched `origin/main` at reviewed PR414 merge
  `a1156f0c0c6271643578cb37894026944df2a633`; all nine required checks passed.
  This is the C21 device-consumption merge, not the missing C20 composition
  repair. Merged it into the isolated C25 branch at candidate
  `39f7b6a8b454adb170b010ac280c65d96354278f`, preserving reviewed ancestry and
  leaving the C25 diff relative to current main under the admitted evidence
  prefix.
- Re-pinned the verifier and fixture manifest to `a1156f0`. The reproducible
  and materialized 55,592,960-byte source archive has SHA-256
  `0e8f182d3c58853213cf575e2d7b6e0a125b92fab96ac3c67969b8ab409c841f`.
- At measured candidate `39f7b6a8b454adb170b010ac280c65d96354278f`,
  `verify.py --mode all` returned `ACCEPTED` in 10.4 seconds across 25 bounded
  commands. Source-gap, archive, fixture, AST dependency, allowlist, focused
  normal/race/vet, zero-test, timeout-cleanup, and aggregate-shutdown controls
  passed with no surviving child process.
- PR417 remains rejected at `b061bf852076d4b36526600377ad916751e32b4b` by the
  C20-owned coverage assertion at
  `agent-cli/internal/wire/composition_test.go:417`; PR412 still has no reviewed
  C20 repair on main. Next action: retain C25, wait for the reviewed same-task
  C20 repair, then refresh exact-head evidence and submit the changed task to
  script CI without polling.
- Fresh read-only reproduction on the merged current-main candidate
  `74ef3dae3f3eeb07f7ebdaffbd2a47d24cbf5b7c` reran the same composition test
  with coverage instrumentation: PASS in 0.573 seconds at 4.7% package
  coverage. The test observed the expected live-media-unavailable error; this
  confirms the hosted C20 coverage rejection remains external and does not
  repair or transfer ownership of `composition_test.go:417`.

Current-head verifier checkpoint
- After re-fetching `origin/main=a1156f0c0c6271643578cb37894026944df2a633`,
  candidate `70f2496f16b8bf036cbe654d99a121ac0a1346f7` ran
  `python3 docs/temp/projects/audio-runtime/audio-runtime-c25-audio-device-boundary-diagnosis/verify.py --mode all`.
- The verifier returned `ACCEPTED` in `14.582s` across `25` bounded commands:
  `SOURCE_GAP_CONFIRMED`, archive/fixture/AST/allowlist checks, focused normal
  and race regressions, vet, zero-test discovery, and child-hang cleanup all
  passed with no surviving child process.
- Audited production paths remain unchanged and the source archive is
  `55,592,960` bytes with SHA-256
  `0e8f182d3c58853213cf575e2d7b6e0a125b92fab96ac3c67969b8ab409c841f`.
- This is an evidence-only refresh. The latest hosted CI rejection remains the
  C20-owned coverage failure at `agent-cli/internal/wire/composition_test.go:417`
  on PR417 head `b061bf852076d4b36526600377ad916751e32b4b`; C25 will not submit
  unchanged source. Next action: C20 reviewed repair reaches `origin/main`,
  then C25 refreshes exact-head evidence and submits the changed same task to
  script CI without polling.

Exact-head verifier refresh at the resumed candidate
- After fetching `origin/main=a1156f0c0c6271643578cb37894026944df2a633`, the
  isolated branch was clean at candidate
  `0a623e78f5d56aa75e5438995da57c1a5c891fe7`. Required startup integration
  `8bdafc7f947a3a2c9856220abdc539437035bd21` and baseline
  `3194edd97aed588f7cdf2f8c58a69ac21da4c9ad` ancestry remain preserved; no
  predecessor, host checkout, or unrelated worktree was changed.
- `rtk proxy python3 docs/temp/projects/audio-runtime/audio-runtime-c25-audio-device-boundary-diagnosis/verify.py --mode all`
  returned `ACCEPTED` in `17.849s` across `25` bounded commands. The source
  gap, archive/fixture digests, AST dependency controls, complete allowlist,
  focused normal/race/vet regressions, zero-test and child-hang controls all
  passed with clean shutdown and no surviving child process.
- Generated diagnosis/provenance and checkpoint metadata now consistently
  identify `0a623e78f5d56aa75e5438995da57c1a5c891fe7`. The source-only finding
  remains the compiled legacy room mixer host-ticker/raw-codec/direct-sink
  dependency; no production/shared/module/fixture/baseline/device/provider/
  realtime path was modified.
- The latest CI rejection remains C20-owned at PR417 head
  `b061bf852076d4b36526600377ad916751e32b4b`, run `34413969081`, job
  `102674534021`, `composition_test.go:417`; fetched `origin/main` still has no
  reviewed C20 repair. Next action is commit/push this evidence checkpoint and
  update PR417, then retain ownership until the reviewed C20 repair reaches
  main; only then refresh against the changed source and submit the same task
  to script CI without polling.

Exact pushed-head evidence refresh
- Fresh `origin/main` remains `a1156f0c0c6271643578cb37894026944df2a633`; no reviewed C20 composition repair is available to integrate. At pushed `HEAD` `50cab0a75732f7d669d8d0ec232a1f854db30368`, `verify.py --mode all` returned `ACCEPTED` in `11.975s` across 25 bounded commands. `SOURCE_GAP_CONFIRMED`, archive/fixture/AST/allowlist, focused normal/race/vet, zero-test, child-hang cleanup and aggregate shutdown controls passed; no child survived.
- Regenerated `diagnosis.json`, `diagnosis.md` and `provenance.json` now identify the exact pushed head. Updated `ownership.json`, `extraction-plan.json` and `ci-diagnosis.json` to the same candidate. No production/shared/fixture/baseline/device/provider/realtime path changed.
- The latest hosted rejection remains PR417 head `b061bf852076d4b36526600377ad916751e32b4b`, run `34413969081`, job `102674534021`, C20-owned `agent-cli/internal/wire/composition_test.go:417` with nil instead of `ErrLiveMediaUnavailable`. Do not submit unchanged C25 evidence; next action is to commit/push this exact-head refresh, update PR417, and retain the same task until the reviewed C20 repair reaches `origin/main`, then refresh against that changed source and submit to script CI without polling.

Resumed current-main checkpoint and C26 DSP re-evaluation
- Admission was reverified for the sole admitted project/task. Fetched
  `origin/main=431fc96c14f0e0045629d9c36f98ee61ff06e840` was merged only into
  the isolated worktree at candidate
  `2574dc27bef53828ac4503047b3d6036ec68cc50`; baseline
  `3194edd97aed588f7cdf2f8c58a69ac21da4c9ad` and startup integration
  `8bdafc7f947a3a2c9856220abdc539437035bd21` ancestry remain intact.
- Reviewed C20 merge `b0acab1238d1aa6bf6bce5ca074451310c7eb039` is on current
  main. Its former `composition_test.go:417` coverage assertion passes in the
  focused local control; this does not claim hosted CI green. C20 retains the
  separate `test46/provider_burst` timeout as historical, unwaived ownership.
- Accepted C26 merge `1f82284abee0bd31a6680310444cea2e4c16ef00` is on current
  main. The C25 verifier now requires the shared bounds/mix calls and records
  `ELIMINATED_BY_ACCEPTED_C26`; no legacy accumulation/final-clipping loop is
  present. The remaining selected gap is the legacy host ticker, PCM format
  adaptation, and direct `DeviceSink.WriteFrame` at
  `session_room_run.go:1186`.
- The bounded command
  `rtk proxy python3 docs/temp/projects/audio-runtime/audio-runtime-c25-audio-device-boundary-diagnosis/verify.py --mode all --source . --source-revision 431fc96c14f0e0045629d9c36f98ee61ff06e840 --child-timeout-seconds 55 --aggregate-timeout-seconds 600`
  returned `ACCEPTED` at candidate `2574dc27bef53828ac4503047b3d6036ec68cc50`
  in `15.518s` across `27` commands, with `10` focused entries accepted and
  no surviving child process. Source gap, archive, fixture, AST dependency,
  allowlist, zero-test, timeout-cleanup, and aggregate-shutdown controls all
  passed. Archive: `56,913,920` bytes,
  `e6f17306b3baf55d44511108871c9ec0546d0c5e3a4639deafc05dd85abf4c31`.
- C25 remains evidence-only and changed paths remain under the owned folder.
  Next action is commit/push, update PR417 with this exact evidence, and submit
  the changed same task to the script CI gate without polling; retain ownership
  for any exact rejection.

Current exact-head refresh after current-main integration
- Historical checkpoint references above remain preserved and are not current.
  Fresh `origin/main=c95a2cb4f96fa8c14bd4655f5197a822c86a980c` was merged into
  the isolated C25 branch at `b957dd96224f76a4919960413d4a6dcbc1dc6bb1`.
  The verifier/manifest repair was committed at exact candidate
  `eeaecc1204da731c7e31469c68100dabc4b3560f`.
- `verify.py --mode all` at that exact committed head returned `ACCEPTED` in
  `14.508s` across `27` bounded commands, including `10` focused normal/race/
  vet regressions. `SOURCE_GAP_CONFIRMED`, archive/fixture/AST/allowlist,
  zero-test and child-hang cleanup controls passed; no child survived. The
  rebuilt/materialized source archive is `57,067,520` bytes with SHA-256
  `5e8cb07b1bc2ef3b999ca57458100bb6620697a0af481c28436493f8a3737a12`.
- The focused current-main composition regression with coverage passed in
  `0.555s` at `4.5%` statements. The hosted PR417 coverage rejection remains
  historical and C20-owned; its separate `test46/provider_burst` failure is
  unwaived. No CI-green, runtime, physical-device, acoustic, or project
  completion claim is made.
- Next action: synchronize remaining metadata, commit/push, update PR417, and
  submit the changed same task to the script CI gate without polling; retain
  ownership for any exact rejection.
