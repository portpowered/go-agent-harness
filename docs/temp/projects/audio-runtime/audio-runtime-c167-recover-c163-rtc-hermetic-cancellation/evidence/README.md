# C167 cancellation-publication evidence

This evidence records the admitted C167 handoff, the preserved C152 operational
candidate and the deterministic before/after browser cancellation-publication
check. The C163/C152 ancestry, PR 512 and the historical hermetic failure remain
unchanged; this slice changes only the three C167-owned browser runner/run/test
files plus this evidence directory.

`cancellation-publication.json` records the exact pre-repair merge experiment,
the channel/barrier oracle, focused normal/nomicrophone/race results and the
bounded accumulated regressions. `browser-cancellation.cover.out` is the
nomicrophone focused coverage artifact generated by the PRD command; it is
ignored by the repository's `docs/temp/` policy and is retained locally for
the handoff.

Nine-lane SCRIPT CI, independent review, guarded merge and the meta-owned
immutable engineering vertical probe remain external C167-04 gates. This
evidence does not close AUDIO, DEVICE, EMBED, SERVICE, TRACE, REPLAY,
FAILURES, QUALITY or PARITY.
