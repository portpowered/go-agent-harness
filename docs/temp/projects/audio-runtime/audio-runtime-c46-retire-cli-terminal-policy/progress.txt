Task: audio-runtime-c46-retire-cli-terminal-policy
Project: audio-runtime
Implementation checkpoint: 1e743f3e2d7ae3559c3c143939c9e4982abcef44
Integrated/tested checkpoint: 9894a18401bb5f4ca6a43b92093f6f671ab06656

CI rejection repair checkpoint
- Canonical task feedback and PR #438's completed static job identify the exact old-head failures: golangci-lint reported the unused `rateLimitRetryDelayPattern` variable; staticcheck 2026.1 additionally reported unused `parseRateLimitRetryDelay` and `legacyStatusDetailField` at `session_rate_limit_retry.go`.
- The two unused wrappers were removed because `rg` found no callers or tests; the provider policy remains the sole parser/normalizer/eligibility implementation. The pre-existing regex global was then retained as an explicit liveness reference because its shared architecture-baseline entry is outside this task's `ownedPaths`; no shared baseline mutation or linter waiver was added.
- Focused provider normal/race tests (19/19 each) and CLI rate-limit/scheduled/cancellation/metadata regressions (27/27 each) passed before the baseline-symbol liveness adjustment. `make fmt`, `make wire-check`, `make coverage-registration`, `make test-architecture-gate`, and provider package tests (67) passed on the candidate.
- The first post-removal architecture check correctly reported only the stale shared baseline entry for `rateLimitRetryDelayPattern`. After restoring the existing baselined symbol, the follow-up analyzer/architecture commands could not complete because the shared volume exhausted its reserve: `df -k .` reported 202376 KiB available and Go reported `no space left on device`. No source, peer artifact, evidence, or shared cache was deleted.
- No new CI/review feedback exists beyond the exact PR #438 static rejection; PR #438 has no reviews. This remains an implementation checkpoint, not CI, review, merge, vertical acceptance, or project acceptance.

Admission and ancestry
- project-control.py verify-work --type task --name audio-runtime-c46-retire-cli-terminal-policy: admitted.
- prd.branchName matches codex/audio-runtime-c46-retire-cli-terminal-policy.
- Fetched origin/main at aa31d6d0e7261f7dcfc07e8f14a7ad29501f542b; baseline 3194edd, startup integration 8bdafc7, manifest intended main ddebb8 and fetched main are ancestors.
- Remote main advanced through reviewed C45 to 7f73c8b3; merged it with --no-ff as 9894a184. The merge changed only C45-owned evidence files, and no C46 owned path conflicted.
- Worktree is clean after the source checkpoint; no host checkout reset or merge was used.
- Canonical board has no C46 rejection feedback or prior review attempt. C38/C40/C45 leases remain disjoint.

Implementation
- Moved terminal status normalization, explicit error metadata precedence, legacy field extraction, rate-limit eligibility/cancellation discrimination, and decimal retry-delay parsing into go-agent-runtime/services/providers/internal/terminalpolicy.
- Added providers.TerminalPolicy and the dedicated providers/wire terminal entrypoint. The existing generic provider Wire graph is unchanged.
- Retained CLI helper names/constants as thin delegates; scheduler, wait, diagnostics, response lifecycle and continuation ownership remain in CLI.
- Registered only the new private terminalpolicy coverage floor at 80.00.
- Added standalone GOWORK=off consumer, positive behavior report and wrong-oracle negative control under this owned evidence directory.

Focused evidence
- Provider terminalpolicy normal/race tests PASS; private coverage 95.5%.
- CLI rate-limit, scheduled retry, tool continuation, cancellation, metadata and non-rate-limit regressions PASS normal and race.
- Focused provider/CLI vet PASS.
- Wire check PASS; coverage registration PASS (175 packages); architecture-size PASS (185 packages, 1893 files, 27796 functions).
- Standalone consumer PASS; mutated delay oracle exits nonzero as expected. Exact public YUI replay was not run; deterministic local provider/session fixture is the bounded route used here.
- After the main merge, provider/CLI normal and race regressions, standalone consumer/negative oracle, Wire, coverage-registration and architecture-size checks all passed again.
- No script CI, independent review, guarded merge, post-merge vertical acceptance or project completion is claimed.

Next action
- Push this changed candidate, open/update the same task PR, and return ACCEPTED to the script-owned CI gate. Do not poll CI; repair any exact rejection on this same task and retain ownership with CONTINUE.

Dead-baseline retirement repair checkpoint
- Current-head CI run `34548179094` / static job `103105220727` was inspected from the job log. The sole failure was `nolintlint` on the unused `gochecknoglobals` directive attached to the dead `rateLimitRetryDelayPattern`; staticcheck completed without findings. The exact prior rejection was not retried unchanged.
- Repair commit `40a3fd582aa830e4f0ac04331c0976489a42058b` removes the obsolete CLI `regexp` import, regex declaration, liveness reference and compatibility comments, and deletes only the matching `mutable-global` entry from `docs/architecture/architecture-size-baseline.json`. The distinct `rateLimitRetryDelayPattern` entry in `docs/architecture/architecture-policy.json` remains unchanged and is owned by the live runtime policy.
- Focused normal tests pass (`1075` tests across the provider policy and CLI agent-runtime packages); focused race policy/rate-limit/scheduled tests pass (`72` across both packages). A full-package race run had one unrelated timeout in `TestBrowserConversationCommandValidatorReadsBoundedStructuredVerdict`; no affected terminal-policy test failed.
- `make fmt`, `make wire-check`, `make coverage-registration` (`175` packages), `make architecture-size-check` (`185` packages, `1893` files, `27794` functions), pinned `make lint` (golangci-lint `2.9.0`, `0` issues) and pinned `make staticcheck` (`2026.1`) pass. Free space after bounded checks was `3064532 KiB`, above the required 2 GiB reserve.
- Updated candidate evidence to tested revision `40a3fd5` with CLI source SHA256 `d4d9a6a1648a660c6801250a9211907666931f307d3f9cb01f1c7445b794ae07` and baseline SHA256 `f2b91bb24e20b42a8a00dbc48efd5560969c29de2177008ba5a880012d159038`. No script CI success, independent review, guarded merge, post-merge vertical acceptance, physical/acoustic proof or project completion is claimed.

Next action
- Commit this evidence checkpoint, push the same PR, update PR #438 with the exact repair and test evidence, and return `ACCEPTED` to the script-owned CI gate without polling it. Retain this task for any exact current-head CI rejection.

# C46 owned consumer oracle and final handoff checkpoint 2026-09-10

- Re-read the required Factory policy/handoff, current C46 PRD, progress and
  complete canonical board/PR inbox. Admission remains the sole `audio-runtime`
  task; `project-control.py verify-work --type task --name
  audio-runtime-c46-retire-cli-terminal-policy` returned `admitted`, the
  isolated branch still matches `prd.json.branchName`, and fetched
  `origin/main=7f73c8b3` is an ancestor together with baseline `3194edd9`,
  startup integration `8bdafc7f`, and planned main `aa31d6d0`. No host checkout,
  peer worktree, predecessor checkpoint, second project or waiver was changed.
- The exact prior CI static rejection remains repaired by `40a3fd5`: the dead
  CLI regex/import/liveness reference and exactly one matching mutable-global
  baseline entry are gone; the distinct live-policy exception in
  `architecture-policy.json` is untouched. No review rows or additional C46
  rejection findings exist in the canonical inbox.
- Added the previously ignored owned consumer test matrix in commit
  `75d0cfcf18620b605673a0a17c182187a9f1f8ed`. Provider policy normal/race,
  independent consumer normal/race, deliberate wrong-delay and
  wrong-eligibility oracles, CLI rate-limit/scheduled/continuation normal/race,
  Wire, coverage-registration, size, architecture, pinned golangci-lint
  2.9.0, pinned staticcheck 2026.1 and diff checks pass. Clean-C46 architecture
  dimensions remain `185` packages, `1893` files and `27794` functions.
- A generated-header/Wire-isolation experiment was tested and rejected by the
  architecture gate because C46 is not authorized to register a new generated
  file in `architecture-policy.json`; those two dirty Wire files were restored
  to the green c861 checkpoint. The separate ignored helper scripts remain
  untracked and were not claimed. Unrelated dirty livehost request/test files
  remain unstaged and preserved for the live-session owner.
- A later public loopback run at source label `c861d1ff` observed failed then
  completed responses, marker `c46 retry recovered`, 16 provider events,
  two response.create messages, clean child reaping and an 8-byte output, but
  it included the preserved unstaged livehost retry opt-in. It is therefore
  not clean C46 source proof and is explicitly not claimed as such; primary
  must probe the exact merged executable after the live-session ownership is
  reconciled. The C46 provider/consumer/CLI behavior evidence remains valid.
- Storage preflight is `149428292 KiB` available, well above the required 2 GiB
  reserve; no new artifact build or cache expansion was performed in this
  checkpoint. Candidate evidence records the new test hash and treats the
  evidence update as a docs-only descendant of `75d0cfc`.

Next action
- Commit the evidence update, push the same PR #438, update it with the exact
  `40a3fd5` static repair and `75d0cfc` consumer-oracle map, then return
  `ACCEPTED` to script-owned current-head CI without polling. Keep ownership
  through `CONTINUE` for any exact rejection; do not stage the preserved
  livehost files or claim review, merge, vertical acceptance or project
  completion.
