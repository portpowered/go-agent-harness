## Summary

- Extracts unresolved tool-result and image/ordinary tool-continuation snapshot construction, normalization, metadata, and typed error joining into the public `go-agent-runtime/services/sessioncontinuation` contract with a private implementation and dedicated Wire package.
- Leaves the CLI lifecycle file as a 48-line explicit-Deprecated compatibility adapter; the immutable baseline was 147 lines, for 99 net physical production lines retired.
- Keeps compatibility identity at the public contract boundary and enriches an unresolved snapshot once, so one obligation produces one typed lifecycle error rather than a duplicate legacy join.
- Preserves the reusable live runtime's sentinel/type compatibility through canonical `errors.Is`/`errors.As` behavior and the historical CLI test facade, while keeping public continuation sentinels immutable.
- Adds a credential-free external consumer, bounded causal/replay evidence, and coverage registrations for the new packages.
- Integrates freshly fetched current `origin/main` (`b8650efd95f6a2e2e675a3dfd0969a9d311a877b`) with no reset in merge `5d5bfe72c9f2d7495e08ca4ef2c05f2b37cc0c94`; the C111 verifier now pins that current-main revision while preserving the released C111 Wire registry entry and fail-closed scope.

## Evidence

- Admission is `admitted` for `audio-runtime` in `~default`; `prd.branchName` matches `codex/audio-runtime-c111-retire-cli-tool-continuation-lifecycle`, and startup, accepted-main, and current-main ancestry are preserved without resetting the host checkout.
- Current-main owned verifier: `verify.py --mode positive-and-negative-controls` accepted at `5d5bfe7`; artifact `artifacts/verify-1789318268-83995.json` (SHA-256 `480539363b42ca789424b349135cb3040579671900cd2aad174db7cd0e826bf9`). The owned-path verifier also accepted at `artifacts/verify-1789318235-83849.json` (SHA-256 `edace4984a35679949b1721c8a49d56151ea8c51308b56fde7047ff943a96c0a`). These verify the 147-line immutable baseline/hash, 48-line adapter, exact released `scripts/wire-packages.txt` entry, owned scope relative to freshly integrated main, runtime contract/race checks, GOWORK=off external consumer, and all four accumulated regressions; every subprocess exited 0, was bounded, and was reaped.
- Final-head focused checks pass: the four named accumulated regressions, GOWORK=off external consumer, and C111 public contract/Wire packages pass; the contract/Wire packages and unchanged peer split-delta characterization also pass under `-race`, and the source-pinned replay covers the image/corrupt controls.
- Source-pinned credential-free replay at `d9e1458`: accepted in 30.275s; artifact `artifacts/run-1789316605-29143.json` (SHA-256 `c142c45ab46facdc3f5c07b35c10cd33303ff4e8c0f43636cbfcf4bf036afb45`). The YUI build exited 0 at 51,123,010 bytes (SHA-256 `a6d6ee2222cd3cd19d799f5027cd1ed656c4c4a0efc4fc4c3ff4e83b95809f64`); missing-tool and corrupt-audio controls passed, tool replay returned `PROBE_TOOL_MARKER_9182`, non-tool replay had no tool events, both audio outputs were produced, all process groups were reaped, and realtime sessions were 0.
- The final quality gates passed format, vet, pinned golangci-lint 2.9.0, pinned staticcheck 2026.1, Wire (13 packages), architecture-size-check (202 packages/1,938 files/28,756 functions), focused contract tests, internal lifecycle tests, and `git diff --check`. Coverage registration passed at the pre-merge checkpoint for 188 packages across 6 modules; the current-main rerun identifies only unchanged C127 package `go-agent-runtime/services/session/internal/live/causal`, outside the C111 manifest.
- The complete prior CI rejection is retained as raw `ci-rejection-34759945739.json`: run `34759945739` at old head `3a88aef2` failed only integration, coverage, and hermetic on the shared image-continuation assertion; all other required jobs passed. The exact log error was a token-limit continuation error followed by the missing image continuation sentinel assertion.
- The prior exact CI rejection remains retained as `ci-rejection-34763400175.json`: run `34763400175` at `bf05d90c` failed integration on `TestAgentBinaryTest46HighRateToolAudioRegression/trial_10` with `167991/174391` remote-device samples rendered (6400 lost), and coverage on unchanged `agent-cli/internal/services/agentsession` at `61.80%` versus its `80.00%` floor. Script CI run `34764898583` then returned the task on pre-merge head `c1b5f1cdf` via completed hermetic job `103744017535`; its exact failure was the unchanged `TestRunSessionWithAudioOut_PreservesNonFrameAlignedSplitDeltas` at `session_audio_out_test.go:146` (`14` bytes versus `974`). The full job log and SHA-256 are retained in `ci-rejection-34764898583.json`; the final C111 diff against freshly integrated `origin/main` still does not change the audio-output path, and the bounded normal/race reproductions pass `20/20`. No peer path, floor, timeout, or assertion was changed.
- The latest complete Script CI rejection is retained as `ci-rejection-34768795618.json`: run `34768795618` at head `3e9c6e67`, job `103754464594`, failed only `CI (coverage)` on unchanged `agent-cli/internal/services/agentsession` at `61.80%` versus its `80.00%` floor (`delta -18.20%`) after the integration lane completed in `304.260s`. The exact log ended with `make: *** [Makefile:303: coverage] Error 1`; hermetic, integration, race, WebMCP Chrome, macOS, Windows, unit, and static completed successfully. The C111 diff does not change that package or the floor, so no peer repair or waiver was made; Script CI is not claimed green.

## Gate status

Independent review findings at `work-review-22` and `work-review-29` are repaired: the CLI adapter no longer joins a second unresolved-result error, compatibility is decision-free at the public boundary, explicit `Deprecated:` markers remain lint-clean, and the verifier/evidence are current. The architecture gate's mutable-sentinel finding is also repaired in `4921c375`. No self-review was performed.

The candidate is ready for same-task resubmission to the script-owned CI gate on this PR. After the no-reset current-main merge, the exact local `YUI_AUDIO_STRESS=1` `trial_10` characterization remains passed in `41.126s` (subtest `1.96s`), the unchanged audio-output normal/race characterization passed `20/20` in both lanes, and the current-main C111 retirement/contract verifier plus unfiltered continuation race pass. The latest coverage rejection remains a completed, unchanged peer failure outside C111 ownership. Script CI is not claimed green; after push, `ACCEPTED` hands off the changed head without polling. If it rejects on a C111-owned signal, inspect the exact failed checks/logs, repair this same task, and resubmit before requesting a fresh independent review and guarded merge. Project-wide SERVICE, REPLAY, FAILURES, QUALITY, and PARITY gates remain open.
