# C22 CI rejection inspection

- PR: `#413`
- Candidate head: `b8b446288c408b35cb74af70703efd831635bfbc`
- Run/job: `34372156060` / `102535799797`
- Rejected check: `CI (hermetic)`
- Failed test: `TestSessionCLI_DuplexPCMMultiTurnRejectsLaterTurnCommitControls/missing_commit`

The full job log was retrieved before diagnosis. The failure occurs in the
positive harness baseline, before the intended negative-control mutation: six
crossings are recorded, harness B completes turns one and two, and its third
turn emits a transcript but no final audio before the existing two-second
deadline. The job reports `context deadline exceeded` and then the expected
raw-audio write cancellation. The same signature is preserved in the
canonical C09 finding at `factory/docs/c09-operator-findings.md`.

This task changes only `agent-cli/internal/config/revision_test.go` and its
C22 evidence directory. There is no diff from `origin/main` in the failing
integration/service paths. The exact focused test passes on both the candidate
and detached `origin/main` with `CGO_ENABLED=0`, `-tags=nomicrophone`,
`-count=1`, and a 30-second Go timeout. Therefore this rejection does not
identify a C22 causal defect; the integration owner must retain that existing
failure. No unrelated path was edited or waived.
