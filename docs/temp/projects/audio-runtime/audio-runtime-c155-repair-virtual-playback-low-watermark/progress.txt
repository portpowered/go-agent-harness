2026-09-13

Static rejection repair checkpoint:

- The full static rejection for CI run `34783651640`, job `103795073680`, is retained as `ci-rejection-34783651640.json`: only the owned adversarial test drifted to `52/106/222/278` against its immutable `49/101/206/260` baseline; format, Wire, vet, lint and Staticcheck passed.
- Repair commit `66aeb3b2bd787041149469bf2b56e5523d6dbb4f` retains the aggregate test's exact baseline metrics and moves only the deterministic waiter-start handshake into a helper. No production, baseline or unowned path changed.
- Post-repair architecture-size passes (`202` packages, `1940` files, `28759` functions). The causal subtest passes normal `200`, race `100`, `GOMAXPROCS=1` `100`, and `GOMAXPROCS=8` `100`; adversarial normal/race `900/420`, typed/loopback `80`, device package normal/race `2530/759`, full gateway module `285`, vet, fmt, Wire, pinned lint, pinned Staticcheck and diff checks pass.
- Fresh `origin/main` remains `4a1c399ccbb3d780be95eb04316e84b8f11a6646`; accepted main and startup integration `8bdafc7f947a3a2c9856220abdc539437035bd21` remain ancestors. The next action is commit this evidence update, push the same PR #516 changed head, and return `ACCEPTED` to the script-owned CI gate without polling; keep ownership for any exact rejection.

- Admission verified from FACTORY_ROOT for the sole audio-runtime task; branch matches prd.json.
- Fresh origin/main and accepted planning base are 4a1c399ccbb3d780be95eb04316e84b8f11a6646; startup integration revision is an ancestor.
- No C155 review findings exist in the canonical factory server list.
- Baseline exact subtest controls passed locally, but the predecessor Windows log showed the same test’s start/read ordering failure.
- Temporary forced start-late control recorded a nil waiter result at queued=2400 above low=1920; the temporary file was removed.
- Repaired subtest uses a standard context Done handshake to establish waiter throttling before reads. No production file changed.
- Focused, race, scheduler, package, gateway, vet and diff checks passed.
- Implementation checkpoint 87c38b48d7cec36ecb476ac44f0f27fcf97b05f0 was committed and pushed; PR #516 was opened from that exact head.
- The evidence-only follow-up commit records this handoff without changing production behavior. The final pushed head is the exact SHA returned in the task response; script CI must run that head, including Windows portable, and owns all polling.

# C155 current-head causal regression recheck 2026-09-13

- After a fresh `git fetch origin main`, `origin/main` remains
  `97d3dcfb1e97a2611aa26b203a7f893442db4768`. The current pushed source was
  clean at `c54129831c4c4e48e96d6d5912a608358309ea89`; accepted main
  `4a1c399ccbb3d780be95eb04316e84b8f11a6646`, startup integration
  `8bdafc7f947a3a2c9856220abdc539437035bd21`, and current `origin/main` are
  all ancestors. The owned diff remains limited to the adversarial test and
  C155 evidence; `device_virtual.go` and `device_virtual_test.go` are
  unchanged.
- Fresh bounded controls on the current source all exited 0: causal subtest
  `200` normal, `100` race, `100` with `GOMAXPROCS=1`, and `100` with
  `GOMAXPROCS=8`; adversarial scenarios `04`-`20` `900` normal and `420`
  race; and typed/loopback regressions `80`. `git diff --check` is clean.
- This remains executor handoff evidence only. No script-CI, independent
  review, guarded merge, vertical acceptance, physical/acoustic proof or
  project completion is claimed. Exact next action is to commit/push this
  evidence checkpoint, keep PR `#516` bound to the exact head, and return
  `ACCEPTED` to the script-owned current-head gate without polling; retain
  ownership through `CONTINUE` for any exact rejection or actionable repair.
