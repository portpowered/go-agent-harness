# C117 runtime-live trace publication repair

This directory contains the bounded evidence and verification drivers for
`audio-runtime-c117-repair-c113-public-trace-publication`. The admitted task
repairs one demonstrated host publication seam; it does not close any broad
audio-runtime criterion.

The immutable C113 executable is `artifact-0` with SHA-256
`27896a6907f5c8e02e185acf9338f07d38e56ebe21896d0b1a740ecde1f2a20d`, built
from planning/current main `3963bc3566da24f8214634c17a9d0f79a6724171`.
`reproduce_c113.py` runs it against the pinned credential-free C16 fixture
under a bounded child process and records the positive replay effects plus the
missing `audio-trace/timeline.jsonl` artifact. The source archive is an
external immutable probe input and is verified by its recorded SHA-256; it is
not rebuilt or substituted here.

Pre-C112 evidence is deliberately fail-closed. The owned livehost test must
remain red until the reviewed C112 public `sessiontrace.Service` is available
and C117 composes that dependency. No C112 implementation, peer Wire package,
shared registry or broad CI result is copied into this directory.

All observations in this slice are process, software-replay, file-receipt or
simulated-callback evidence. Native Windows hardware/endpoints and physical
acoustics are `OUT_OF_SCOPE_AND_NEVER_PASS`.

