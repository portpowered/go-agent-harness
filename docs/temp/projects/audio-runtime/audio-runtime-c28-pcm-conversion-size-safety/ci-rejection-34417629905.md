# C28 CI integration rejection diagnosis

- Run: `34417629905`; failed job: `102685924130`; check: `CI (integration)`; head: `7d772d30e749f777440d7ad9f422b55d9d8e89b7`; URL: <https://github.com/portpowered/go-agent-harness/actions/runs/34417629905/job/102685924130>.
- The complete failed-job log was read from GitHub. The job ran `make test-integration`, which ran `go test ./test/integration -timeout 480s`. The only actual test assertion reported by the package was:

  ```text
  --- FAIL: TestFamilyAIterativeBuildUpThroughShippedProcess (0.25s)
      s2s_conversational_customer_sim_family_a_test.go:139: output evidence reads=3 bytes=12, want four streamed confirmation audio markers
  FAIL github.com/portpowered/go-agent-harness/agent-cli/test/integration 282.663s
  agent-cli test command exited with status 1
  make: *** [Makefile:255: test-integration] Error 1
  ```

- The other replay, invalid-fixture, credential, voice and provider-close messages in the log are emitted by the package's existing negative-control tests; no additional `--- FAIL:` assertion appears in the completed job log.
- C28's converter call sites are limited to `session_room_replay_scheduler.go` and `session_room_run.go`; Family A is `probe.RunDuplexSession` and does not call `ResamplePCM16` or `ConvertPCM16Bytes`.
- Candidate reproduction: `rtk go test ./agent-cli/test/integration -run '^TestFamilyAIterativeBuildUpThroughShippedProcess$' -count=20 -timeout 120s` from `7d772d30` passed 20/20.
- Baseline reproduction: the same command from detached `origin/main` (`a1156f0c0c6271643578cb37894026944df2a633`) passed 20/20. The baseline worktree was removed after the comparison.
- C28 causal checks remain green: `rtk go test ./go-audio/pkg/audio -count=5 -timeout 120s` passed 785 tests; the exact current-head consumer/probe was refreshed separately. This rejection is therefore a CI-only baseline-equivalent integration miss outside the admitted C28 production/test lease, not a demonstrated C28 defect.

No out-of-scope integration test or runtime path was changed. The evidence refresh below records the exact current candidate head before the same PR is resubmitted to script-owned CI.
