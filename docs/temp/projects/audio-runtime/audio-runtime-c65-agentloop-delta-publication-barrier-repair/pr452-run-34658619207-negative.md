# Inherited negative evidence

- Source PR: [PR 452](https://github.com/portpowered/go-agent-harness/pull/452)
- Run: [34658619207](https://github.com/portpowered/go-agent-harness/actions/runs/34658619207)
- Head: `8e8177c031a7b3b9322d712af19970e13fa7a1bc`
- Workflow result: failed
- Failed job: `CI (hermetic)` / `Run hermetic test pipeline`
- Other run jobs: passed, including unit, race, static, coverage, integration, WebMCP Chrome, Windows audio portable, and macOS audio release.

The immutable failed-job log was inspected with:

```text
gh run view 34658619207 --log-failed
```

The exact failure section was:

```text
2026-09-11T23:41:53.1511879Z --- FAIL: TestRunJoinsPublishedDeltasBeforeReturningOnEngineError (0.00s)
2026-09-11T23:41:53.1515616Z     run_delta_barrier_test.go:82: published provider delta was lost before Run returned
2026-09-11T23:41:53.1516768Z FAIL
2026-09-11T23:41:53.1517609Z FAIL github.com/portpowered/go-agent-harness/go-agent-loop/pkg/agentloop 0.865s
```

The first divergence was the test oracle: it waited for `Deltas().Len() >= 1` while a prequeued `SESSION.OPEN` could occupy the first public slot, then injected the terminal error before proving that the intended text delta had reached the public path. The accepted `Run` implementation already had a forwarding join from `f3bf2613`; this task makes the causal interleaving explicit and tests the join under bounded public backpressure.
