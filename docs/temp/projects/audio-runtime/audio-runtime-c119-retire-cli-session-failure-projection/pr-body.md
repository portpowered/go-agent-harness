## Summary

- Extract session failure normalization, provider-close synthesis, cancellation/non-terminal exclusion, output-state derivation, continuation projections, unsupported-tool diagnostics, synchronized first-observation state, and original-error preservation into go-agent-runtime/services/sessionfailure.
- Add the dedicated public Wire seam and retain an exactly 86-line Deprecated CLI forwarding adapter; delete the 246-line legacy implementation.
- Add the separate GOWORK=off consumer, adapter regressions, bounded causal-mutation verifier, and credential-free shipped-yui replay runner.

## Focused evidence

- go test ./go-agent-runtime/services/sessionfailure/... -count=1: 22 passed.
- go test -race ./go-agent-runtime/services/sessionfailure/... -count=3: 66 passed.
- Adapter-focused race tests: 12 passed; combined focused legacy/adapter regressions: 30 passed.
- verify.py --mode positive-and-two-mutations: accepted; both targeted mutants fail with --- FAIL: output.
- External consumer with GOWORK=off: passed and proves Wire isolation, errors.Is, errors.As, defaults, exclusions, close output, and continuation projection.
- run.py --case provider-error --case synthesized-provider-close --case cancellation-negative --case healthy-audio-tool-continuation --child-timeout 60 --aggregate-timeout 240: accepted; pinned fixtures, bounded process groups, provider failure/close terminal manifests, cancellation negative, and healthy audio/tool output/effect.
- session_diagnostics_failure.go is absent; adapter is 86 lines; accepted-main retirement accounting is 246 CLI production lines; excluded caller/shared fingerprints remain unchanged.

## Fresh executor checkpoint

- Exact source head: `ba5337e411d97f932ff426b7d1eed588f1d550ee`; the worktree is clean and the pushed branch/PR #505 point at this head.
- Focused normal/race tables and accumulated named session diagnostics/cancellation regressions pass; the separate `GOWORK=off` consumer passes; `verify.py --mode positive-and-two-mutations` is accepted and both mutants fail their intended semantic oracles.
- `verify.py --mode retirement-and-owned-paths` is accepted. Fresh replay evidence `run-1789274464-93155.json` is accepted from the same source head and rebuilt yui: provider-error and synthesized provider-close surface typed terminal evidence, cancellation-negative remains excluded, and healthy audio/tool continuation produces output/effect; every child is bounded, reaped, and leaves no process group survivor.
- `origin/main` was freshly fetched at `ea53be13ce5e4ef14fd8c89c695c21744a1f7686` and is not yet an ancestor of this candidate. C79 PR #470 remains open and its shared Wire/architecture registries remain leased; no shared-file edit or stale-main integration is claimed.

## Current executor handoff

- Exact source head is `221da3ffed74be69812079ed7c237605cd19d4c7`; the worktree and pushed PR #505 are clean at this head. Task admission reverified as `admitted`; `prd.branchName` matches; no second project or acceptance waiver is present.
- Fresh bounded evidence remains green: service normal `22` tests, service race `66`, external `GOWORK=off` consumer, `11` caller regressions, causal artifact `verify-646.json` with both mutants failing their intended oracles, and retirement/owned-path artifact `verify-647.json` with `86` adapter lines and `246` retired accepted-main CLI lines.
- `COUNT=1 bash scripts/test-session-ci-regressions.sh all` passed normal, coverage, and race modes. Replay artifact `run-1789284027-1152.json` is accepted from this source head and rebuilt yui `6d9ebab3bf94eded5f9e5d5bcb9db43256bc640aba199b6b6bce6891c121db06`; all four credential-free cases passed, all child groups were reaped, and no survivors remained.
- Fresh `origin/main` is `ea53be13ce5e4ef14fd8c89c695c21744a1f7686` and is not an ancestor of this candidate. C79 PR #470 still holds the shared Wire/architecture lease, so integration and shared-file reconciliation remain deferred. No Script CI result, independent review, merge, or vertical acceptance is claimed.

## Static budget repair checkpoint

- The full current-head CI static log for run `34745142367`, job `103691571589`, at submitted head `d905345d094805d3b11457a48cda038494cdddad` had exactly two findings: `agentruntime` was `238 > 237`, and `services/sessionfailure/wire/wire_gen.go` was unregistered.
- The C119-owned `session_failure_projection_test.go` had no callers outside itself and duplicated the service/unchanged-caller coverage. Commit `20fd1e5` deletes that 97-line test-only file; the live agentruntime package is now exactly `237` Go files. The production adapter remains exactly `86` lines, the legacy implementation remains deleted, and no baseline or registry path was changed.
- Post-repair `rtk make architecture-size-check` reports exactly one remaining issue: generated-file-spoof for `services/sessionfailure/wire/wire_gen.go`. Service normal/race tests, the GOWORK=off consumer, unchanged caller regressions, both causal mutants, and the accumulated normal/coverage/race session regressions all pass; `verify.py --mode positive-and-two-mutations` produced accepted artifact `verify-72645.json`.
- C79 PR #470 remains open, mergeable and 9/9 green with no review or guarded merge. The remaining Wire registration and any released-main baseline reconciliation stay deferred until C79 explicitly releases its shared ownership. This checkpoint is not yet pushed or submitted to Script CI.

## Handoff

This head is based on accepted main 3963bc3566da24f8214634c17a9d0f79a6724171 and preserves startup ancestry. scripts/wire-packages.txt and docs/architecture/architecture-size-baseline.json remain untouched because C79 still holds their active lease; C119-05 must integrate current origin/main only after C79's reviewed guarded merge and explicit release. The executor has not polled or claimed Script CI green.

## Final post-C79-release executor checkpoint

- C79 PR #470 was guarded-merged at `1a8467246c6607a06ffc7289075da2595724ce8b`. After a fresh fetch, `origin/main` is that revision; it was integrated into this isolated branch with a no-fast-forward merge commit `d7a5e7322e0b06cb734fd23c0f94908027806ad`. Startup `8bdafc7f947a3a2c9856220abdc539437035bd21`, accepted main `3963bc3566da24f8214634c17a9d0f79a6724171`, and current main ancestry all verify.
- The C119 static repair remains commit `20fd1e544`: only the redundant 97-line `session_failure_projection_test.go` was removed, bringing `agentruntime` to exactly 237 Go files. The released sharded architecture baseline remains unchanged; no limit was raised and no coverage floor was weakened.
- After C79 release, commit `6fae9457331bcbec95bbc6a06323ab9b5d64f8b0` adds only the demonstrated `sessionfailure` generated-Wire rule to `docs/architecture/architecture-policy.json` and the matching `scripts/wire-packages.txt` entry. The verifier now scopes candidate mutations against current `origin/main`, validates that exact shared diff, and still proves the immutable 246-line accepted-main source and startup admission.
- Final bounded gates are green: `make fmt`, `make vet`, pinned golangci-lint `2.9.0`, pinned staticcheck `2026.1`, coverage registration (182 packages), coverage/floor (182 registered packages across 7 profiles), Wire (10 graphs), architecture/size (192 packages, 1,911 files, 28,218 functions), and `git diff --check`.
- `verify.py --mode final` is accepted in `verify-29328.json`: service normal/race checks, isolated `GOWORK=off` consumer, unchanged caller regressions, and both causal mutants have the required results. The accumulated session regression script passed normal, coverage, and race modes at `COUNT=1`.
- Fresh source-pinned replay `run-1789288132-31227.json` is accepted from source `6fae9457331bcbec95bbc6a06323ab9b5d64f8b0` with yui SHA256 `87740daba99c4f8b0f42c6b7820e7a3f1ae153b8380df0344c635fbfad62ea5c`; provider error, synthesized provider close, cancellation-negative, and healthy audio/tool continuation all passed their bounded observable oracles with zero Realtime sessions and no surviving process groups.
- This executor has not polled or claimed Script CI, independent review, guarded merge, or vertical acceptance. The next action is push this same candidate to PR #505 and return `ACCEPTED` for the script CI gate.

## CI rejection characterization

- Script CI run `34747817964` job `103698870781` rejected source `494e91e106d2bd21ce5db5a78b28c71164259d44` only in `CI (integration)`, at `TestAgentBinaryTest45HighRateToolAudioRegression/trial_18`: `171191/177591` rendered samples, `6400` lost, `0` dropped/overflow/discarded samples, and `34` underflow events. The exact failed-step log and structured record are retained in `ci-integration-rejection-34747817964.json`.
- After a fresh fetch of `origin/main=1a8467246c6607a06ffc7289075da2595724ce8b`, the exact 20-trial Test45 command passed in `18.362s`; the complete bounded `make test-audio-device-server-integration` target also passed in `105.114s`, including Test45/Test46 and the companion process/device cases. No C119-owned audio, device, transport, or integration path changed, so no C119 production repair is demonstrated or applied; the peer high-rate audio failure remains explicit and unwaived.
- C119 normal/race service tests, GOWORK=off consumer, unchanged caller regressions, and accumulated normal/coverage/race session regressions remain green. This is a changed evidence head for one script-CI resubmission; it does not claim CI green, independent review, guarded merge, vertical acceptance, or project acceptance.

## Current-main integration and static rejection checkpoint

- The current-head static rejection for PR #505 is preserved in `ci-static-rejection-34748733564.json`. The completed job metadata identifies `Run golangci-lint` as the failed step; the exact pinned local reproduction was `session_duration_terminal.go:26` (`exhaustive`), a C120-owned file outside C119. GitHub did not expose the job log while the overall run was still in progress; no finding is omitted or waived.
- Fresh `origin/main` is `09c70f51243caeaf1184c4806b99bbf7749e3044`, the guarded C120 merge. The same C119 branch integrated it as merge commit `1dbcea900eb10b5392f008ca328075d0ee5e250d`; startup, accepted-main, and current-main ancestry verify, and the C119 production diff remains limited to the admitted paths. The C120 stale exhaustive-switch finding disappears without a C119 source repair.
- Post-merge focused gates are green: pinned `make lint` (0 issues), `make fmt`, `make vet`, pinned Staticcheck 2026.1, `make wire-check`, coverage registration (`185` workspace packages), coverage/floor (`make coverage-changed`), and architecture-size (`195` packages, `1,917` files, `28,277` functions). C119 service normal/race tables, the GOWORK=off consumer, the 11 unchanged caller regressions, both causal mutants, and `verify.py --mode final` are green.
- Rebuilt merged-source yui replay artifact `run-1789291551-27227.json` is accepted with binary SHA-256 `ca183bb84ee50730c0526042c9541faf0444a901c60670de533c957e28bc9826`: provider failure, synthesized provider close, cancellation-negative, and healthy audio/tool continuation all pass, with `PROBE_TOOL_MARKER_9182`, bounded/reaped process groups, zero Realtime sessions, and no survivors.
- This checkpoint claims no CI green result, independent review, guarded merge, vertical acceptance, or project acceptance. The next action is to commit/push this post-merge evidence, update PR #505, and return the changed head to script CI without polling; retain the same task for any exact new rejection.

## Current-main conflict recovery and final executor handoff

- The canonical task rejection returned PR #505 at head `a3f45ff43933f8a19c860525d77ced260fe684fd` because GitHub marked it mergeable `CONFLICTING`. After a fresh fetch, current `origin/main` is `071b0abfd67501db61e3c1929971c6dd6e77eb62`. The isolated task branch merged it without reset as `22b21863d`; the sole conflict was the shared generated-Wire policy list, resolved by retaining both C119 `sessionfailure` and current-main C116 `rtctransport` rules. Commit `54b150ed` restores the expected current-main-then-C119 rule order. The shared Wire registry retains both corresponding entries; no architecture limit or coverage floor changed.
- Required ancestry is verified for startup `8bdafc7f947a3a2c9856220abdc539437035bd21`, accepted main `3963bc3566da24f8214634c17a9d0f79a6724171`, and current `origin/main`. The worktree is clean at source head `54b150ed8bd2fe54d20aa1762a5be926683f9978`; the C119 diff against current main remains confined to the admitted C119 paths plus its released shared registration.
- `verify.py --mode final` is accepted in `verify-18038.json`: service normal/race tables pass, the `GOWORK=off` external consumer passes, all unchanged caller regressions pass, and both causal mutants fail their intended semantic oracles. The accumulated `COUNT=1 bash scripts/test-session-ci-regressions.sh all` command passes normal, coverage, and race modes with the existing negative controls preserved.
- Bounded quality gates pass: `make fmt`, `make vet`, pinned golangci-lint `2.9.0` (`0 issues` in all 15 modules), pinned Staticcheck `2026.1`, `make coverage-registration` (188 packages), `make coverage-changed COVERAGE_BASE=3963bc3566da24f8214634c17a9d0f79a6724171`, `make wire-check`, `make architecture-size-check` (`198` packages, `1,925` files, `28,323` functions), and `git diff --check`.
- Fresh source-pinned replay `run-1789298445-18993.json` is accepted from `54b150ed`, with yui SHA-256 `ca183bb84ee50730c0526042c9541faf0444a901c60670de533c957e28bc9826`: provider-error and synthesized-provider-close produce their expected typed terminal records, cancellation remains a no-failure negative path, and healthy audio/tool continuation produces `PROBE_TOOL_MARKER_9182` plus `strict replay continuation`. The four child groups are bounded/reaped with zero survivors, and Realtime sessions are `0`.
- This is executor evidence only. Script CI has not been polled or claimed green; independent review, guarded merge, fresh vertical acceptance, and project acceptance remain external. The next action is to push `54b150ed` on the same PR #505 and return `ACCEPTED` to the script-owned CI gate without polling; retain this task for any exact rejection.

## Final repair and current-main executor handoff

- Candidate source head is `10580eea7f54d6595e926481868881977635590b`, following repair checkpoint `a65e16145` and isolated no-fast-forward integration merge `bb807f11f` of current `origin/main=bd6a1289218d1bef1a3af36e64e9d4496062416f`. Accepted-main `3963bc3566da24f8214634c17a9d0f79a6724171` and startup `8bdafc7f947a3a2c9856220abdc539437035bd21` ancestry remain preserved; the running host checkout was not merged or reset.
- The latest independent rejection is repaired: one `sessionfailure/wire.Invocation` is cached in the observer lifecycle, the private service owns first acceptance/snapshot/clear and synchronized state, the adapter only forwards/converts, callback reentrancy and original error identity are covered, and nil compatibility inputs remain safe. The legacy `session_diagnostics_failure.go` is absent, the adapter is exactly 86 lines, and accepted-main retirement remains 246 CLI production lines; excluded callers and released shared baselines remain preserved.
- Final bounded gates pass: `make fmt`, `make vet`, `make wire-check`, the latest focused CLI race suite, and `verify.py --mode final` artifact `verify-20956.json`. The verifier records service normal/race repetitions, the isolated external `GOWORK=off` consumer, unchanged caller regressions, and both causal mutants failing their intended semantic oracles.
- Fresh source-pinned software replay artifact `run-1789305753-21456.json` is accepted from the rebuilt yui SHA-256 `c47146542f5605aec5d25685fcc2c0d91beda171db248e27e88e72c5669c4daf`: provider-error, synthesized-provider-close, cancellation-negative, and healthy-audio-tool-continuation all met their bounded observable oracles; Realtime sessions were `0`, every child was reaped, and no process-group survivor remained.
- Historical Script CI integration rejection `34747817964` remains retained as an unwaived peer high-rate audio loss (`6400` samples); no C119 audio/device path was changed. This executor has not polled or claimed Script CI green, independent review, guarded merge, vertical acceptance, or project acceptance. Next action: push this same candidate and update PR #505 for the script CI gate without polling; retain the task for any exact rejection.

## Final static-repair executor handoff

- Script CI run `34759774506` rejected PR #505 at submitted source head `3dfbe215b37d068bb5b4c11b75d2848deec06e16`; failed static job `103730446876` identified the architecture-size and golangci-lint steps. The GitHub log was unavailable while the overall run was still in progress, so the exact local reproductions were retained: architecture-size reported the agentruntime baseline drift `238 > 237`, mutable global `wire/invocation.go:projectionKinds`, and Wire business methods `Accept`, `AcceptClose`, `AcceptError`, and `Clear`; lint reported the adapter magic number `3`.
- Repair commit `df491b215a72b602fdbcf7731acd174766cb48a2` deletes the redundant 72-line adapter test, restoring the `237`-file package budget; moves `AcceptError`, `AcceptClose`, `SnapshotFacts`, and publication rollback into the public contract/private service; leaves Wire as a constructor/embedding seam with no mutable projection table or business methods; and leaves the Deprecated adapter as typed conversion/forwarding at exactly `86` lines. No caller file, released shared Wire registration, architecture baseline, coverage floor, or unrelated change was modified.
- Exact committed-source verifier artifact `verify-70430.json` is accepted: service normal/race tables, the isolated `GOWORK=off` consumer, unchanged caller regressions, and both causal mutants have the required results. `make fmt`, `make vet`, pinned golangci-lint (`0` issues), `make wire-check`, coverage registration (`188` workspace packages), architecture-size (`198` packages, `1,928` files, `28,648` functions), and `git diff --check` pass.
- Exact committed-source replay `run-1789307488-70432.json` is accepted from `df491b215a72b602fdbcf7731acd174766cb48a2`, with rebuilt yui SHA-256 `c47146542f5605aec5d25685fcc2c0d91beda171db248e27e88e72c5669c4daf`; provider-error, synthesized-provider-close, cancellation-negative, and healthy-audio-tool-continuation all met their bounded oracles, the run was credential-free with `0` Realtime sessions, and every child was reaped with no survivor. `COUNT=1 bash scripts/test-session-ci-regressions.sh all` then completed with the normal, coverage, and race accumulated regression suites passing, including the preserved negative controls and 20-trial regression.
- This is executor evidence only: no current Script CI green result, independent review, guarded merge, vertical acceptance, or project acceptance is claimed. The next exact action is push the same task to PR #505, update this handoff, and return `ACCEPTED` to the script-owned CI gate without polling; retain ownership for any exact CI rejection.

## CI rejection 34761314555 repair

- Script CI rejected source `a338dceb29bc39f95fd27700bad8d7803a4db318` in both `CI (coverage)` job `103734536659` and `CI (hermetic)` job `103734536697`. The exact failures were `TestSessionProgressObserver_WatchdogTimesOutExactlyOnce` and `TestSessionProgressObserver_ClassifiesExplicitEmptyPartialResponse`: the adapter emitted generic `unknown`/CLI/session-run facts instead of the typed watchdog or explicit-empty session facts. The complete characterization is retained in `ci-rejection-34761314555.json`.
- Commits `19393b9ec` and `666e518d` repair the demonstrated cause. The adapter asks the per-observer `sessionfailure` service first and, only when it has no accepted observation, reads the legacy liveness latch under `livenessMu`; projection forwarding remains typed, nil-safe, caller files remain unchanged, and the adapter remains exactly `86` lines.
- Current-head validation is green: focused normal (`13`), focused agentruntime race (`24`), service normal (`54`), service race (`5`), isolated `GOWORK=off` consumer, accumulated normal/coverage/race session regressions, format, vet, pinned lint (`0` issues), pinned Staticcheck, coverage registration (`188` packages), Wire, and architecture-size (`198` packages, `1,928` files, `28,648` functions). Fresh credential-free replay `run-1789310048-43348.json` is accepted from rebuilt yui SHA-256 `67949e223de59d8114380afd3bc16dfc745a45c7188cbcb0e7bed0b1d1747ddc`; Realtime sessions are `0` and all children were reaped.
- The inherited `progress.txt` checkpoint from `a338dceb2` is preserved in Git history and this owned evidence/handoff, but its root-file diff is excluded from the candidate. Final verifier artifact `verify-52019.json` is accepted at source head `d67705291b181fe0f98aee3415e51e91b3755c23`; no ownership waiver is introduced. This executor has not polled or claimed Script CI green, independent review, guarded merge, vertical acceptance, or project acceptance.

## Final nil-safety repair and Script CI handoff

- Script CI run `34763656307`, coverage job `103740708513`, rejected the prior head `3e6453d266551c1772325909e94d2111ef0d33de` on `TestSessionCommand_ReplayCorruptedProducedCaptureReportsNestedDivergence`: the observed error was `context deadline exceeded`, while the test expected `replay mismatch`. The exact focused normal and coverage reproductions passed at `count=3`; this is outside the C119-owned projection paths and no unrelated integration code was changed. Historical peer high-rate audio loss `34747817964` remains retained and unwaived.
- Commit `9e7f62b86fca903741c2b6eee71377413c3bf305` restores the adapter's compatibility nil guards for observation, close, and legacy failure state. The cached per-observer `sessionfailure` invocation remains the sole lifecycle owner; the adapter remains Deprecated forwarding/conversion at exactly `86` lines, the retired accepted-main CLI source remains `246` lines, and no caller or released shared baseline changed.
- Exact committed-head verifier artifact `verify-9452.json` is accepted: service normal/race checks, the isolated `GOWORK=off` consumer, unchanged caller regressions, and both causal mutants have the required results. The accumulated `COUNT=1 bash scripts/test-session-ci-regressions.sh all` passes normal, coverage, and race modes; `make coverage-changed COVERAGE_BASE=3963bc3566da24f8214634c17a9d0f79a6724171` exits `0`; and the bounded credential-free replay `run-1789312546-6786.json` accepts provider-error, synthesized-provider-close, cancellation-negative, and healthy-audio/tool-continuation with zero Realtime sessions and no process survivors.
- Production repair commit `9e7f62b86fca903741c2b6eee71377413c3bf305` and the final handoff documentation commit `33ba54121bdac6a5256fe200bbf0a0dd262e7b3e` are pushed. Admission, exact `prd.branchName`, startup/accepted-main/current-main ancestry, and a clean worktree remain verified. This executor claims no Script CI green result, independent review, guarded merge, vertical acceptance, or project acceptance. The exact next action is submit this same candidate on PR #505 to the script-owned CI gate without polling; retain this task for any exact new rejection.

## Current-main merge and bounded executor handoff

- Fresh `origin/main=915ed982d23f2e549e529ff43c4f370b4b51e394` was integrated without reset as no-fast-forward merge `096c6d0ac80c7967aae85c66befa081985329d87`. Startup `8bdafc7f947a3a2c9856220abdc539437035bd21`, accepted main `3963bc3566da24f8214634c17a9d0f79a6724171`, and current main are ancestors; the C119 diff remains limited to the recorded C119 paths and released shared registration. The running host checkout and predecessor worktrees were not touched.
- On the merged tree, the C119 service normal/race tables, GOWORK=off consumer, 11 unchanged caller regressions, both causal mutants, `verify.py --mode final` artifact `verify-74475.json`, and `verify.py --mode retirement-and-owned-paths` artifact `verify-74470.json` pass. The adapter remains exactly 86 lines, the legacy 246-line implementation is absent, and retirement remains 246 accepted-main CLI production lines.
- `COUNT=1 bash scripts/test-session-ci-regressions.sh all` passes normal, coverage, and race modes, including the preserved high-rate/replay/tool-lifecycle controls and expected negative diagnostics. `make fmt`, `make vet`, pinned golangci-lint 2.9.0, pinned Staticcheck 2026.1, `make wire-check`, `make architecture-size-check` (`199` packages, `1,929` files, `28,671` functions), `make coverage-changed COVERAGE_BASE=3963bc3566da24f8214634c17a9d0f79a6724171`, and `git diff --check` pass.
- `make coverage-registration` is blocked only by the current-main C127-owned package `go-agent-runtime/services/session/internal/live/causal`, which has no coverage-manifest entry. C119 does not own that package or absorb the peer repair; the exact prerequisite is retained for the C127 executor (`work-task-3`) to add its manifest coverage and rerun the gate.
- Fresh merged-source replay artifact `run-1789314936-70739.json` is accepted from `096c6d0ac` and rebuilt yui SHA-256 `2f6634a5867249a74fdac12f22e3afb4d8c17fb127cfc257f138ac2f7c64700d`: provider-error and synthesized provider-close produce typed terminal evidence, cancellation remains excluded, and healthy audio/tool continuation emits `PROBE_TOOL_MARKER_9182` and `strict replay continuation`. All four child groups are bounded/reaped, zero process-group survivors remain, and Realtime sessions are `0`.
- No Script CI result has been polled or claimed, and no independent review, guarded merge, vertical acceptance, or project acceptance is claimed. The candidate is pushed for PR #505 continuity, but C119 remains `CONTINUE` until the C127-owned coverage-manifest prerequisite is resolved; then rerun `make coverage-registration`, refresh exact handoff evidence if needed, and submit this same changed head to script CI without polling.

## Hermetic rejection 34768071404 and bounded resubmission

- Script CI run `34768071404`, job `103752527911`, rejected source `b073776dac88ec0c05d29c738f24e7b7beb983d9` only in `CI (hermetic)`, during `make test-hermetic` with `CGO_ENABLED=0` and `nomicrophone`. The exact failure was `TestAgentBinaryToolContinuationPreservesRemoteDeviceAudio/test48_matched_healthy_control/provider_burst`: the remote playback deadline expired before the final PCM marker (`rendered_pcm=467040`, `nonzero_pcm=99835`, `expected_pcm=120795`, `final_marker=false`) while playback reported zero queued, dropped, overflow, discarded, or discard-event samples. The complete evidence is retained in `ci-hermetic-rejection-34768071404.json`.
- The exact subtest passed locally with the hermetic environment at `count=1` (exit `0`, `11.956s`) and three bounded repeats (exit `0`, `27.424s`). The current C119 verifier is accepted in `verify-59071.json`; its service normal/race tables, isolated `GOWORK=off` consumer, unchanged caller regressions, and both causal mutants pass. `COUNT=1 bash scripts/test-session-ci-regressions.sh all` also passed normal, coverage, and race modes, including the 20-trial high-rate tool-audio regression and preserved negative controls.
- The failed integration test is outside every admitted C119 prefix, and no C119-owned cause or production repair is demonstrated. The independently accepted C138/C127 terminal-drain report covers a distinct fixture and does not authorize changing excluded audio, device, transport, or integration paths. This checkpoint records the exact rejection without a waiver; a repeat rejection requires an exact ownership/repair decision for that excluded path before any broader change.
- After fetching current `origin/main=b8650efd95f6a2e2e675a3dfd0969a9d311a877b`, the isolated branch integrated it with no-fast-forward merge `b0e8cebbc5f08dfadfaaf5038b712f3567ecd5bc`; the shared Wire conflict retained both `sessionfailure` and `sessiontrace` registrations. The merged-tree verifier is accepted in `verify-72024.json`, the accumulated normal/coverage/race matrix passes, and the exact failed subtest passes three more times (`28.738s`). `fmt`, `vet`, Wire, and architecture-size also pass. `make coverage-registration` remains deterministically blocked only by the peer C127 causal package’s missing manifest entry, so C119 remains `CONTINUE` pending an exact owner/release of that prerequisite; no acceptance waiver is introduced.

## Fresh executor verification and retained peer prerequisites

- Admission was reverified with `project-control.py verify-work --type task --name audio-runtime-c119-retire-cli-session-failure-projection`: `status=admitted`, project `audio-runtime`, contract `audio-runtime-v1`; `prd.branchName` and the isolated branch match exactly. Startup `8bdafc7f947a3a2c9856220abdc539437035bd21`, accepted main `3963bc3566da24f8214634c17a9d0f79a6724171`, and current `origin/main=915ed982d23f2e549e529ff43c4f370b4b51e394` remain ancestors of source `a7459900468a0f28cec132cbb8c7d4f851145a73`; the worktree was clean before this evidence checkpoint.
- Fresh `verify.py --mode final` artifact `verify-84595.json` is accepted. The service normal table, race table, isolated `GOWORK=off` consumer, seven unchanged caller regressions, and both causal mutants pass their required controls; `drop-terminal-facts` and `convert-cancellation-to-failure` both fail their intended semantic oracles. The adapter remains `86` lines, the legacy source remains absent, accepted-main retirement remains `246` CLI production lines, and the verifier reports only admitted C119 paths plus the released Wire registration.
- The accumulated `rtk proxy env COUNT=1 bash scripts/test-session-ci-regressions.sh all` matrix passes normal, coverage, and race modes. It preserves expected replay/PCM/transcript negative diagnostics, passes the 20-trial high-rate audio/tool regression in each mode, and passes the device/provider regression packages.
- A rebuilt source-pinned yui from `a7459900468a0f28cec132cbb8c7d4f851145a73` has SHA-256 `2f6634a5867249a74fdac12f22e3afb4d8c17fb127cfc257f138ac2f7c64700d`. Replay artifact `run-1789315603-90141.json` accepts provider-error, synthesized-provider-close, cancellation-negative, and healthy-audio-tool-continuation; it records zero Realtime sessions, bounded/reaped children, and no surviving process groups.
- Fresh bounded quality gates pass: `make fmt`, `make vet`, pinned golangci-lint `2.9.0` (`0 issues`), pinned Staticcheck `2026.1`, `make wire-check`, `make architecture-size-check` (`199` packages, `1,929` files, `28,671` functions), and `git diff --check`, plus focused room-replay normal/coverage characterization (`count=3` each). The full `make coverage-changed COVERAGE_BASE=3963bc3566da24f8214634c17a9d0f79a6724171` run retains one non-C119 room replay failure, `TestRunRoomWithResultReplaysAdmittedBundleWithoutLiveConfiguration`, which did not reproduce in the bounded focused reruns and is not claimed fixed by C119.
- `make coverage-registration` still fails deterministically only because current main contains the peer package `go-agent-runtime/services/session/internal/live/causal` without a coverage-manifest entry. C119 does not own that package or its manifest and does not absorb the repair. This checkpoint therefore remains executor `CONTINUE`: retain the same task/PR, have C127/work-task-3 add and release the manifest registration, then fetch/integrate the accepted current main, rerun coverage-registration and any affected bounded gates, and submit the changed head to script CI without polling. No CI green result, independent review, guarded merge, vertical acceptance, or project acceptance is claimed.
