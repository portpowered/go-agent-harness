# C91 current-main revalidation — 2026-09-12T13:33:56Z

## Identity and ancestry

- `project-control.py verify-work --type task --name audio-runtime-c91-retire-cli-capture-claim-runtime --root "$FACTORY_ROOT"` returned `{"status": "admitted", "project": "audio-runtime", "name": "audio-runtime-c91-retire-cli-capture-claim-runtime"}`.
- The isolated branch is `codex/audio-runtime-c91-retire-cli-capture-claim-runtime`, matching `prd.json.branchName`.
- `git fetch origin main` observed `origin/main=d4766c3dbbf2c198142047ead4449d58dd47d485`; it contains only the accepted factory successor-contract anchor repair relative to the prior C91 base and no C91-owned or C79-shared paths.
- Current `HEAD=00f37caac8eb85a01098f58342d1490a8ca244ce` is a clean merge of that current `origin/main`, with startup integration `8bdafc7f947a3a2c9856220abdc539437035bd21` and admitted baseline `59af6325614d80173447fe2018a0471e27b4e7b1` as ancestors.
- The C91 legacy baseline remains `317` lines with SHA-256 `1747b40578f4788b1b698495652f7e8f85abbff278eab904581cd38edcc8b6cc`; the adapter remains `77` lines, retiring `240` lines.

## Causal and accumulated verification

- `rtk proxy python3 docs/temp/projects/audio-runtime/audio-runtime-c91-retire-cli-capture-claim-runtime/verify.py --mode all` passed at `00f37caa`, including boundaries, failure matrix, retirement/adapter, mutation, owned-path and excluded-path checks.
- `COUNT=1 rtk bash scripts/test-session-ci-regressions.sh all` passed normal, coverage and race modes. The accumulated matrix passed the CLI audio-interrupt ordering controls, shipped record/replay and duplex controls, tool continuation and high-rate audio trials, simulated-device controls, and composed OpenAI provider controls; expected negative-control diagnostics remained asserted.
- The current-head verifier now explicitly rejects a candidate that does not contain the fetched `origin/main`, closing the stale-ancestry false-pass path without relaxing any assertion or scope.

## Ownership and handoff

- The canonical `~default` board has C91 as `work-task-160` in `init`, with no C91 review row or rejection. C79 `work-task-114` remains `ci-pending`; PR #470 is open and unmerged, so C79 still exclusively owns `scripts/wire-packages.txt` and `docs/architecture/architecture-size-baseline.json`.
- No shared registry/baseline file was edited. No current-head SCRIPT CI result, independent review, guarded merge, immutable vertical probe or project acceptance is claimed.
- After C79's guarded merge releases both exact leases, integrate the then-current accepted `origin/main`, add only the demonstrated captureclaim Wire registration and three stale `acquireSessionRecordingClaim` baseline deletions, rerun bounded gates, push/update PR #481, and submit that changed head once to SCRIPT CI without polling.
