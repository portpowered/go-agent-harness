# C110 current-head CI rejection

- Run: `34745856689`
- Candidate: `9c9115cd674d817b0facbb4de5217aa99d5bcc7e`
- Pull request: `497`
- Branch: `codex/audio-runtime-c110-retire-cli-session-instruction-resolution`
- Base: `ea53be13ce5e4ef14fd8c89c695c21744a1f7686`
- Failing jobs: `CI (static)` job `103693559462`; `CI (hermetic)` job `103693559487`

## Static

`make architecture-size-check` found exactly five findings in 189 packages:

1. The C110 legacy baseline entry for
   `RunSessionWithInstructionsAndAudioOutAndTextSeedAndMaxDuration` is stale
   for each of `cognitive-complexity`, `cyclomatic-complexity`,
   `function-lines`, and `function-statements`.
2. `services/sessioninstructions/wire/wire_gen.go` has a generated header but
   is not yet registered with a reproducible generator.

The four stale entries are in the shared architecture-baseline ownership held
by C79. The generated-file registration is likewise a shared registry repair.
The C110 task must not edit either shared registry until C79's reviewed guarded
merge explicitly releases them.

## Hermetic

All preceding hermetic packages passed. The sole failing test was the unrelated
high-rate provider-audio residual:

`TestAgentBinaryToolContinuationPreservesRemoteDeviceAudio/test46/provider_burst`

The hosted run timed out while the child was still running with
`rendered_pcm=467520`, `expected_pcm=174391`, `final_marker=false`,
`callbacks=974`, zero reported drops/overflow/discard counters, and
`inputSamplesSeen=701279`. This path is outside C110's instruction-service
lease; the failure is preserved as peer-owned negative evidence and is not
waived or relabeled.

## Local reconciliation

- Sessioninstructions focused normal: 16 tests passed across 3 packages.
- Sessioninstructions focused race: 45 tests passed across 3 packages.
- GOWORK=off external consumer: passed.
- Positive/guard-mutation verification: accepted; both causal mutants were killed.
- Retirement/owned-path verification: accepted; legacy adapter is 87 lines and
  the measured CLI retirement is 190 lines.
- Accumulated session regressions (`COUNT=1`, normal/coverage/race): passed,
  including high-rate, duplex, simulated-device, and composed-provider checks.
- `make wire-check`: passed and generated output was unchanged.
- Local `make architecture-size-check`: reproduced exactly the same five
  shared C110/C79 findings above.

## Required next action

C79 `work-task-25` remains `in-review` with `work-review-31`; PR 470 is open at
`b5e11e47d5ca43b46ed875cabc1418939270ebc5` and has no guarded merge. After
that review and guarded merge release the shared registries, fetch and
integrate the current `origin/main`, apply only the four demonstrated C110
baseline deletions and the sessioninstructions Wire registration, rerun the
bounded C110 gates, push this same PR 497, and submit its changed head to
script CI without polling.
