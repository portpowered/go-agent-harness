# C139 current-head revalidation — 2026-09-13T17:37:37Z

## Admission, identity, and ownership

- `rtk proxy python3 factory/scripts/project-control.py verify-work --type task --name audio-runtime-c139-recover-c91-capture-claim-runtime` from `FACTORY_ROOT` returned `{"status": "admitted", "project": "audio-runtime", "name": "audio-runtime-c139-recover-c91-capture-claim-runtime"}`.
- The isolated branch is `codex/audio-runtime-c139-recover-c91-capture-claim-runtime`, exactly matching `prd.json.branchName`.
- The clean candidate is `2fe4074249e406842196155f1d2d9bf12fa8ddba`; refreshed `origin/main` is `4a1c399ccbb3d780be95eb04316e84b8f11a6646`. The candidate is 9 commits ahead and 51 behind this current main.
- Startup integration `8bdafc7f947a3a2c9856220abdc539437035bd21` and planning base `915ed982d23f2e549e529ff43c4f370b4b51e394` remain ancestors of the candidate. No reset, rebase, force-push, host-checkout mutation, predecessor rewrite, or peer-worktree mutation was performed.
- The live `~default` board retains C139 as `work-task-56` on this worktree. C110 (`work-task-26`) and C119 (`work-task-19`) remain `in-review`; their shared registry leases are not released. C139 has no prior review row or review finding.

## Exact candidate checks

- `rtk gh pr view 514` reports PR 514 `OPEN`, non-draft, `MERGEABLE`, at exact head `2fe4074249e406842196155f1d2d9bf12fa8ddba`.
- Existing run `34768895501` has 8 of 9 required checks successful. `CI (static)` is the only failure and reports exactly the three stale `acquireSessionRecordingClaim` architecture-baseline entries plus the unregistered `go-agent-runtime/services/captureclaim/wire/wire_gen.go`. This is shared-lease evidence, not a C139-owned repair authorization.
- `rtk proxy python3 .../verify.py --mode all` exits 1 at the fail-closed current-main ancestry check because `origin/main=4a1c399c` is not yet an ancestor of this preserved candidate. It does not establish a product failure; the required current-main integration is deferred until the shared leases release.
- Focused normal captureclaim/CLI tests pass: `231` tests in 4 packages at `-count=3`.
- Focused race captureclaim tests pass: `54` tests in 3 packages at `-count=3`.
- Focused CLI compatibility tests pass: `12` normal tests at `-count=3` and `4` race tests at `-count=1`.
- The `GOWORK=off` external consumer passes (`example.com/capture-claim-consumer`, `0.212s`).
- `COUNT=1 scripts/test-session-ci-regressions.sh all` exits 0 in normal, coverage, and race modes. Expected mismatch and negative-control diagnostics remain asserted by the passing matrix.
- `git diff --check` passes and the worktree remains clean. No `scripts/wire-packages.txt` or `docs/architecture/architecture-size-baseline.json` change is present.

## Handoff

The preserved C139 implementation and evidence remain actionable, but the only remaining candidate failures are the demonstrated shared Wire/baseline repairs. Retain C139 ownership. After C110 and C119 complete guarded merges and explicitly release the two shared paths, fetch the then-current accepted `origin/main`, merge it in this worktree, apply only the captureclaim Wire registration and the three demonstrated downward baseline deletions, rerun the bounded verifier and focused gates, commit and push PR 514, then submit that changed head once to SCRIPT CI without polling. Do not claim CI, review, merge, vertical acceptance, or project completion before those external gates.
